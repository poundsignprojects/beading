# CLAUDE.md — Bead Pattern Designer

This file gives Claude Code persistent context for this project. Read it at the start of every session. Update it (especially the Phase Status section) as work progresses, so the next session picks up accurately.

## Project Overview

A custom web app replacing a commercial app the user previously used for designing seed bead patterns (peyote, brick stitch, square stitch, bead looming). Built from scratch — not a clone, not reverse-engineered — based on a feature list and workflow requirements gathered directly from the user. Runs in Safari on iPad (installable to home screen), used for personal, non-commercial purposes only.

## User Context

- Professional web developer background, and experienced working with AI coding tools (has used Cursor, Cline).
- Primary device for actual use: iPad (with Apple Pencil). Development happens on a Mac laptop.
- Uses zsh for shell commands.
- Wants to be asked before large code/content output — explain intent first, then proceed on confirmation. (This applies to chat collaboration generally; use judgment on how it maps to normal Claude Code workflow.)
- Wants a professional, honest recommendation first, even if it's more work — not the easiest path. Discuss modifications after that, not instead of it.
- No hyperbole, no unnecessary compliments/praise.

## Why This App Exists (origin of requirements)

The prior app's specific pain points that this app must fix:
1. Preferences don't persist across designs — must set grid/tool preferences per-file, every time.
2. No manual control over the order designs are displayed in a library view.
3. Colorways (same pattern, different color mapping) require full duplicate files — wasteful and error-prone.
4. Easy to lose current state — no autosave, must remember to manually duplicate before experimenting.
5. Closing/switching designs takes too many steps.

## Core Decisions Log

| # | Decision |
|---|---|
| 1 | Platform: web app (HTML/CSS/JS, no framework), not native — installable to iPad home screen via Safari. Native (SwiftUI + Xcode + Cursor/Cline) is a possible future path if wanted later, but web app ships faster and de-risks the design/logic decisions first. |
| 2 | No UI component library (no React, Tailwind, shadcn, etc.) — interface is mostly custom canvas interaction; plain HTML/CSS is sufficient and keeps the dependency surface small. |
| 3 | Rendering: HTML5 Canvas for the grid, not DOM elements per bead (patterns can be thousands of cells — canvas handles zoom/pan/redraw far better). |
| 4 | Storage: IndexedDB for local persistence (not localStorage — size limits and synchronous API are a problem at pattern scale). |
| 5 | Backup/cloud: not required for v1. Plan is manual export/import of a JSON file first (simple, user controls the file), real sync layered on later once the data model is stable. |
| 6 | Internal units: always millimeters (mm), matching the bead spec source data. Imperial (inches) is a **display-layer-only** conversion — never store or calculate internally in inches. This must be built into Phase 1, not retrofitted. |
| 7 | Bead types in scope: Miyuki Delica and Miyuki Rocaille only, both starting at size 11/0. Toho and Preciosa are out of scope for now. |
| 8 | Colorways = same grid, different color-to-cell mapping only. Not different bead counts or sizes. |
| 9 | Draw tool = two interactions: (a) tap for single bead placement, (b) drag/continuous stroke for line drawing across cells. Both required, not just one. |
| 10 | Photo-to-pattern auto-conversion is explicitly NOT a priority. Photo trace overlay (reference image, adjustable transparency, user draws over it manually) IS wanted — much simpler feature, don't conflate the two. Layering clarified during Phase 7 implementation (2026-08-09): the photo renders as a translucent overlay *on top of* the whole canvas, beads included, at the chosen opacity — not tucked behind beads like tracing paper (the "background" wording here originally implied the latter; superseded, see Phase Status). |
| 11 | First stitch type to build: peyote. Others (brick, square, loom) come later, after peyote's grid engine and tools are validated. |
| 12 | v1 must-have tools, in priority order: draw, erase, undo/redo, print/export instructions. Fill, color-replace, cut/copy, mirror are v1.x — after the must-haves work well, not before. |

## Bead Specs (source: official Miyuki site, miyuki-beads.co.jp/english/seed/01.html — verified via screenshot of their official size chart)

| Type | Size | Diameter (mm) | Hole (mm) |
|---|---|---|---|
| Round Rocaille | 11/0 | 2.0 | 0.8 |
| Delica (DB) | 11/0 | 1.6 | 0.8 |

**Known gap, unresolved as of last session:** Miyuki's official chart gives only one diameter figure per bead — no separate width vs. height. Grid rendering needs both (this is what makes peyote/brick rows render as offset rather than square). Secondary, non-Miyuki sources suggest approximate figures below, but these are **not verified against an authoritative source** and should be treated as provisional until confirmed (e.g. by direct caliper measurement or a second manufacturer source):
- Delica 11/0: ~1.6mm wide × ~1.3mm tall (estimated)
- Rocaille 11/0: ~2.0mm wide × ~1.3–1.5mm tall (estimated, rocailles are flatter than their diameter suggests)

Store these as named, easily-editable constants (not hardcoded magic numbers scattered through grid math) so they can be corrected without a rewrite once verified.

## Architecture

Vanilla JS, ES modules, no build step, no framework, no bundler.

```
/src
  /grid          — grid math: peyote/brick/square/loom coordinate generation, bead aspect ratios
  /render        — canvas drawing, pan/zoom, viewport logic
  /tools         — draw, erase, fill, color-replace, cut/copy, mirror (one file per tool)
  /palette       — bead color data (Delica/Rocaille specs, color libraries)
  /storage       — IndexedDB wrapper, save/load, autosave
  /export        — word chart generation, print layout
  /units         — mm <-> inch conversion helpers (display layer only)
  main.js        — app shell, wires modules together, event handling
index.html
style.css
CLAUDE.md        — this file
```

## Dev/Verification Tooling

- No `package.json`/`node_modules` in this repo — `node --test 'src/test/**/*.js'` run directly from the repo root runs the whole `node:test` suite (glob quoted so the shell doesn't expand it; `node --test src/test/` alone fails with `MODULE_NOT_FOUND`, it needs actual file paths).
- Playwright isn't installed locally either, but it's globally npx-cached (confirmed present via `npx --no-install playwright --version`, currently 1.62.x) — `npx playwright ...` or a script requiring `'playwright'` both resolve fine without any install step first. No need to search for or install it each session.
- Standard verification loop used for every phase so far: `python3 -m http.server <port>` from the repo root (serves `index.html` at the repo root directly, no build step), then a throwaway Node driver script using `playwright` to drive headless Chromium against `http://localhost:<port>/`. Exact bead-cell screen coordinates are computed by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`/`worldToScreen`'s math directly in the driver script (or via dynamic `import()` of the app's own modules in-page), not approximated — see any past phase's Phase Status verification notes for the pattern. Kill the http.server background process when done.

## Best Practices To Follow

- Grid math and tool logic as pure functions (state in, state out) — separate from canvas rendering and DOM/event handling. Keeps core logic testable and reasoning-friendly across sessions.
- Internal units always mm. Conversion to inches happens only at the point of display (readouts, labels) — never propagate converted values back into stored/calculated state.
- One central app-state object; modules read/write through defined functions, not by reaching into each other's internals.
- Comments explain *why*, especially for grid offset math — not restating *what* the code visibly does.
- Commits should map to phase boundaries (see below), so repo history documents the build progression.
- Bead physical specs live as named constants in `/palette`, not inline magic numbers, since at least one dimension is still provisional (see Bead Specs gap above).

## Phase Plan

1. **Phase 1 — Peyote grid engine.** Grid math only: given bead width/height ratio, generate correct offset-row peyote layout, render on canvas, support pinch-zoom/pan. No drawing yet. mm-based internally, inches toggle for size readout (Decision #6) built in from the start.
2. **Phase 2 — Draw + erase.** Tap placement, drag-to-draw (Decision #9), erase, Delica/Rocaille color palettes wired in.
3. **Phase 3 — Undo/redo.** History stack over draw/erase actions (Decision #12). Built as its own phase, ahead of save/load, since it's core drawing-tool functionality tightly coupled to Phase 2's draw/erase — not lumped in with the lower-priority tools in Phase 7.
4. **Phase 4 — Save/load + project library.** Autosave to IndexedDB, reorderable design list (manual ordering), global persistent preferences (fixes prior-app pain point #1 and #2).
5. **Phase 5 — Print/export instructions.** Row-by-row word chart generation, printable layout.
6. **Phase 6 — Colorways.** Same-grid/alternate-palette variant (Decision #8), lightweight relative to a full duplicate (fixes prior-app pain point #3).
7. **Phase 7 — Remaining tools.** Fill, color-replace, cut/copy, mirror, photo trace overlay with adjustable transparency (Decision #10). (The grid/canvas orientation toggle originally listed here was dropped as of the Phase 7 planning session, 2026-08-09 — the user confirmed the underlying complaint was already resolved by the row/col transpose fix done between Phases 5 and 6, and the item was a misunderstanding of that same issue, not a separate need.)
8. **Phase 8 — Side panel layout + custom color palettes.** Restructure the editor from one wrapping top toolbar into a slim top bar + narrow left tool rail + wide right-side color/colorway/photo-trace panel, structurally modeled on the prior app's own layout (screenshot reviewed 2026-08-09) though not its specific icons/styling (not a clone, per this file's Project Overview). Alongside that, replace the placeholder color swatches with a user-built palette — native `<input type="color">` picker + name prompt, stored per bead type in a new `customColors` IndexedDB store — since sourcing the real Miyuki catalogs (900+ SKUs each, no machine-readable hex data) isn't practical; see the Bead Specs section's known gap. Built per `.work/phase-8-implementation-plan.md`, see Phase Status.
9. **Later / optional, not yet scheduled.** Brick/square/loom grid types, cloud sync (Decision #5), native app rebuild if still wanted, bead-type conversion (see note below), thumbnail grid view of library, tagging or folders in library, ability to import the prior app's exports.

**Bead-type conversion (feature request, added 2026-08-09, not yet scheduled into a phase).** Convert an existing design from Rocaille to Delica or vice versa in place. Rows/cols (stitch structure) stay the same; `gridParams` gets re-derived from the new bead type's `widthMm`/`heightMm` (`src/palette/beadSpecs.js`) so cell proportions and the finished-size readout are correct for the new bead. Open design question, unresolved: color remapping. As of Phase 8, `delica11` and `rocaille11` each have their own independent user-built palette (`customColors`, scoped by `beadTypeKey` — see `src/storage/customColorStore.js`), so the "accidentally aliased placeholder array" situation this note originally described no longer exists — the two bead types' color sets are genuinely different from the moment a user adds their first color to each, not just once real Miyuki catalog data eventually lands. This makes the remapping question real immediately, not deferred: this feature still needs a real strategy (match by hex proximity? by name? flag unmapped colors for manual reassignment?) before it can ship. Conceptually adjacent to Phase 6 (Colorways) — both are "same grid, altered mapping" — but distinct: a colorway is the same bead type with a different palette; this is a different bead type on the same grid.

## Phase Status

*(Update this section at the end of each session — what's done, what's in progress, what the next session should pick up.)*

- Status as of last session: **Loomerly PDF pattern import — implemented in full and verified end-to-end against four real sample PDFs, per `.work/feature-loomerly-import-plan.md`.** Also produced one significant side finding, written up but deliberately **not** implemented this session — see its own entry immediately below.
  - **Corrected the plan's reversal-parity guess before writing any real code.** The plan's own geometry section had guessed "odd-numbered Loomerly row reversed, even not" for un-reversing a pair's second half before interleaving. Re-derived from scratch this session using a small hand-built 4×10 ground-truth sample the user provided with genuinely readable direction arrows (rare — see below) and its own picture-chart page: the correct rule is the **opposite** — a pair's first row (always odd-numbered) prints in canonical left-to-right order as-is; its second row (always even-numbered) prints reversed and must be flipped back before interleaving. Confirmed two independent ways: bead-for-bead against the picture chart (Loomerly's own Row 3 text matched its picture-chart row unreversed; Row 4 matched only after reversing), and via a "smoothness" check (average color-run-count per reconstructed row) on a real 4-color tessellation sample — the corrected rule produced visibly smoother, more plausible reconstructions (5.10 avg runs/row) than the plan's original guess (9.45) on the same file. This was the one piece of the plan's "hard part" derivation that turned out to need a real fix, not just confirmation; every other piece (pairing, centering, interleave order, the core rows=Width/cols=Height mapping) held up exactly as derived.
  - **Direction is never read from the printed arrow at all, by design** — confirmed against all four real sample PDFs in `.work/samples/` that pdf.js's `getTextContent()` returns an **empty string** for the →/← glyph (the font's subset has no ToUnicode mapping for it; visually renders fine, just isn't extractable as text). The tiny ground-truth sample above happened to have real readable arrows only because it came from a different/newer Loomerly export than the four main samples — worth remembering if a future sample also has readable arrows, that's not something to rely on generally. Direction is fully derivable from pairing position anyway (see above), so this was designed around rather than worked around.
  - **`src/import/pdfText.js`** (new) — `extractPdfPageTexts(arrayBuffer)` per the plan, backed by a pure, independently-tested `reconstructPageText(items)`. The plan described this as a "thin wrapper, no parsing logic" but real Loomerly PDFs needed one genuine text-layout fix beyond naive same-y-line joining: a color-list page's catalog#/code/name/count columns for *different colors* print at the *same* y-coordinate with no space between them in the raw item stream (confirmed directly on real per-item x/y dumps) — naive line-joining concatenates them into unusable garbage (`"M01M16FM135"`). Fixed with a two-pass reconstruction: cluster items into visual lines by y-proximity first (comparing each item against its cluster's own anchor y, not a sliding last-seen y — a sliding comparison let per-item jitter scramble reading order within one line, caught by a dedicated regression test), then within each line, split into separate output lines wherever the horizontal gap between consecutive items exceeds 20pt (normal word/glyph gaps run under 10pt in this font; real column gaps run 100pt+). This is a generic PDF text-layout concern, not Loomerly-specific parsing, so it stayed in `pdfText.js` rather than `loomerlyParser.js` — confirmed byte-for-byte compatible with the app's real vendored pdf.js output across all four samples (diffed against an earlier single-pass version; only difference was a footer line splitting more cleanly, no regression).
  - **`src/import/loomerlyParser.js`** (new, pure) — `parseLoomerlyExport(pageTexts)` per the plan's shape, plus one real-data-driven addition the plan didn't anticipate: footer lines ("Word Chart"/"Color List" section-heading echoes, "Page N of M", and the pattern's own title) repeat at the *end of every page*, and a multi-page color list's page-boundary footer sits directly between the last word-chart line and the first real color-list line — landing squarely inside the color-list body slice unless excluded. Fixed by filtering `/^Page \d+ of \d+$/`, the two literal heading strings, and (since a title is arbitrary user text with no safe generic pattern to match) any line equal to the *document's own last line* — confirmed on all four real samples that the title reliably reappears as the very last line of the whole PDF. A dedicated regression test (`'color-list body ignores footer echoes...'`) exercises this against a synthetic multi-page fixture built to match the shape of a real page boundary, not just single-page samples where the bug wouldn't have shown up. Section validation ("Section 1 of 1") was downgraded from the plan's assumed hard requirement to a soft check — confirmed against real data that only one of the four samples has that text as extractable at all (it lives in a page footer that most samples never render in an extractable position); absence no longer blocks import, presence still validates.
  - **`src/import/loomerlyImport.js`** (new, pure) — `buildGridFromLoomerly(parsed)`, `guessBeadType(parsed)`, `computeImportWarnings(parsed, grid, beadTypeKey)`, matching the plan's decisions (bead type guessed-and-shown never silent; validation warns, doesn't block, consistent with Phase 5/6's philosophy). `FINISHED_SIZE_TOLERANCE_IN` tuned to 0.5in after checking real data both ways: too tight (0.4) false-flagged two samples whose bead type came directly from a confident header-line read, where the size gap is just this app's already-known, already-tracked provisional bead-height imprecision (see CLAUDE.md's Bead Specs gap) — not a real signal the guess might be wrong; 0.5 stays quiet on those while still catching a genuinely wrong bead-type guess (the deliberately-forced-wrong case in `loomerlyImport.test.js` has a 0.54in gap, still well past threshold).
  - **`/vendor/pdfjs/`** (new) — pdf.js 4.10.38's prebuilt minified browser ESM build (`pdf.mjs` + `pdf.worker.mjs` from npm's `pdfjs-dist`, source-mapped files and the sandbox/forms bundle deliberately left out — not needed here), ~1.7MB total, committed as static files per the plan's decision (this project's first real third-party dependency, a deliberate narrow exception to Decision #2 which was about UI frameworks specifically — worth a follow-up note there next time that file's touched). Verified actually working — not just "the file exists" — via a real headless-Chromium Playwright pass loading `index.html` over a plain local `http.server`, dynamically importing `pdfText.js`, and running the full extract→parse→import pipeline against a real sample PDF fetched over HTTP: exact bead/band/color counts, matching every `node:test` fixture's numbers.
  - **`src/ui/importDialog.js`** (new) — three-step flow (file pick → preview → color assignment) triggered from a new `#library-import` button. One deliberate architecture deviation from the plan's literal wording: the plan's file-by-file breakdown implied a single Promise-returning flow across all three steps, but a native `<input type="file">` picker has no reliably cross-browser "user cancelled" event — wrapping the whole thing (including the initial file pick) in one Promise would hang forever whenever a user opens the picker and backs out without choosing a file. Restructured as `mountImportDialog(db, { onImported })`, mounted once at boot exactly like `editorView.js`'s existing Photo Trace file-picker (same established pattern, same underlying browser limitation), reporting success via callback instead of a resolved Promise; cancelling at any step, including the native picker, just does nothing. Preview step shows computed vs. stated stats, the bead-type guess (overridable via a `<select>`, live-recomputes warnings on change), and any `computeImportWarnings` output as dismissable-but-visible notices, never blocking. Color-assignment step is one row per unique code with a native `<input type="color">` (Phase 8's existing no-default-forces-a-real-choice convention, tracked via a `pickedHex` Map gating the Confirm button rather than relying on the input's own value, since `type="color"` always has *some* value and can't natively express "untouched"); confirm creates one `customColors` entry per code via the existing `customColorStore.createCustomColor`, substitutes real `colorId`s into the grid's `colorEntries`, and writes the finished record via `designStore.createDesign` + `saveDesign` — design name taken from the PDF filename (extension stripped), renamable afterward via the existing library rename action like any other design.
  - `libraryView.js`/`main.js` — new `#library-import` button next to "+ New"; `main.js`'s `handleImported(design)` follows the same in-place `appState.designs` update + immediate `openDesign()` pattern every other library mutation already uses (no full re-fetch).
  - `node:test` coverage: 169 passing (30 new — 6 for `pdfText.js`'s line-reconstruction incl. the column-gap-splitting and same-line-jitter-ordering regressions; 13 for `loomerlyParser.js` incl. the footer-pollution regression and both with/without-bead-type-header-line cases; 11 for `loomerlyImport.js` incl. an exact-fixture regression test built from the tiny ground-truth sample's real numbers, unequal-length-pair interleaving, shaped-row centering verified against a real lattice-sample excerpt, and `guessBeadType`/`computeImportWarnings`). No `node:test` coverage for `importDialog.js` itself (DOM-dependent, same precedent as every other `src/ui/*.js` module) — verified in headless Chromium instead.
  - Verified in headless Chromium (Playwright, local `python3 -m http.server` + driver scripts, real sample PDFs from `.work/samples/` fetched over HTTP — no synthetic/mocked PDF data anywhere in this verification): a clean single-color sample imports with the color-assignment Confirm button correctly gated (disabled until the one code's color is picked, enabled after), lands the exact picked hex on canvas (pixel-sampled off-center to avoid an even-dimension grid's gridline-at-dead-center gotcha, same precedent as prior phases), survives a full page reload with the design still present in the library and reopening to the identical color, and Print/Export opens/closes cleanly on the imported design with no console errors. A 5-color sample with a genuine bead-type header line and a real (expected, sub-threshold) finished-size note showed the correct bead-type guess/source label, all 5 colors in both the preview table and the assignment step, and — the strongest check run this session — an exact bead-for-bead cross-check against IndexedDB's actually-persisted `colorEntries` for one specific physical row (band 22, reconstructed from Loomerly's real `Row 45 (9)D` + `Row 46 (9)D, (1)C`) matched the hand-derived expected sequence exactly (`D,C,D,D,D,D,D,D,D,D,D,D,D,D,D,D,D,D,D` — 18×D, 1×C, C landing at position 1) — confirming correct placement, not just correct aggregate counts. Cancelling at the preview step left the library with zero designs created. Feeding a well-formed-but-non-Loomerly PDF-shaped file through the picker surfaced a clear, dismissable error ("Could not read this PDF...") without corrupting prior library state.
  - **Follow-up fix, same session, from the user's first real import attempt**: the user imported the tiny ground-truth Pattern.pdf through the actual UI (not a test fixture) and got a visibly wrong result — a screenshot showed the very first (top) band reading backwards relative to every band after it, described as "issues with the starting beads." Root cause: `bandFromPair`'s combined-pair case (`pair.length === 1`) returned the printed "Rows 1 & 2" text as-is, on the assumption it was already in canonical order — an assumption that was never rechecked after the general reversal rule got corrected earlier in the session (see above) and turned out to be wrong. The combined line is printed line index 0 (even) and shares the exact same "needs reversing to reach canonical order" direction as every other even-indexed line (Row 4, Row 6, ...) — confirmed against the ground-truth sample's own picture-chart page a second time, the same way the general rule was originally derived: printed `Row 1&2 (1)B,(1)A,(1)B,(1)A` only matches the picture's band-0 rendering (`A,B,A,B`) after reversing. Fixed by reversing it in `bandFromPair`, matching every other even-indexed line. Also added a `computeImportWarnings` check (non-blocking) that flags any file whose `Starting bead` isn't exactly `"Top Right"` — the only value seen across all five samples this reversal convention has been derived from and checked against (four main samples plus this ground-truth one) — since the convention is unverified for anything else.
    - `loomerlyImport.test.js`'s ground-truth fixture test updated (band 0 now asserts `A,B,A,B`, matching every other band's own pattern, instead of the old buggy `B,A,B,A`); new test for the starting-bead warning. 170 `node:test` passing (1 net new).
    - Re-verified against all four main real samples: per-color/total-bead counts and warnings are byte-for-byte unchanged from before this fix (a reversal doesn't change aggregate counts, only position — exactly why this bug survived every count-based check run earlier in the session without a dedicated position-level check on the *combined* band specifically). Re-verified the fix directly in headless Chromium: reconstructed the ground-truth sample through the real browser-loaded `loomerlyParser.js`/`loomerlyImport.js` (no PDF file available in this environment for this specific sample — only its pasted text content — so this seeded a design from the computed grid directly, the same shape `importDialog.js` itself produces, rather than driving the file-picker UI for this one check) and confirmed programmatically that all four screen-columns now form one continuous, correctly-alternating pattern with zero discontinuity at band 0 (previously the very first band broke the pattern) and the expected solid final band — screenshotted the canvas for a direct visual match against the user's own report.
  - **Follow-up feature, same session, user-requested**: the color-assignment step originally always opened the native color picker with no default, per the plan's Phase-8-empty-state precedent — the user asked for the option to instead reuse a color they've already built for that bead type. `renderColorAssignStep` (now async) fetches `listCustomColorsSorted(db, beadTypeSelect.value)` and shows each row's `parsed.colors` entry with two ways to assign a color: the existing palette as clickable `.color-swatch` buttons (identical look/behavior to the editor's own color palette — `aria-pressed` toggling, `style.background`), or the native `<input type="color">` tile for a genuinely new one. Picking an existing swatch stores `{type:'existing', id}` and `handleConfirm` reuses that real `customColors` id directly — no near-duplicate entry created; picking the native picker stores `{type:'new', hex}` and creates a fresh `customColors` entry exactly as before. This directly addresses the plan's own logged "deferred fast-follow" (color reuse across imports) for the common case of a color the user already has in their palette, though it's a manual per-color choice rather than the plan's originally-envisioned automatic catalog-number matching — that automatic-matching idea is still open if wanted later, now on top of a manual reuse path that already works.
  - Verified in headless Chromium: pre-seeded one existing `delica11` custom color, then imported a 3-color sample — the pre-seeded color showed as the only swatch on its matching row, clicking it toggled `aria-pressed` correctly, the other two colors were assigned via the native picker, Confirm enabled once all three had a choice, and after import: exactly one new `customColors` entry existed per newly-picked color (2 new + the 1 pre-seeded = 3 total, not 4 — confirming no duplicate was created for the reused one) and the design's actual `colorEntries` reference the pre-seeded color's real id.
  - **Not yet done**: real iPad pass (file-picker UX for selecting a PDF from Files/iCloud in Safari is untested territory — Phase 4's precedent was Photo Trace's image picker, a different picker UX; the plan flagged this as the last build step). Deferred fast-follows already logged in the plan, still open: fully-automatic color reuse across imports by matching catalog number (manual reuse now covers the common case, see above), non-peyote Loomerly exports, multi-section patterns (still explicitly rejected with a clear error, not attempted).

- Status as of last session: **Word chart doesn't split rows into real thread passes beyond the foundation — found, root-caused, and written up in full, but deliberately left unimplemented this session at the user's explicit request ("if it can be deferred, please just write it up").** Found while building the Loomerly importer above: the user built matching tiny (4×10, 2-color) test patterns in both this app and Loomerly and compared the printed word charts directly, which surfaced a real correctness bug in `src/export/wordChart.js` — shipped, physical-iPad-verified functionality, not something this session introduced. First read on it wrongly concluded the whole grid coordinate model needed restructuring (walked back once checked band-by-band against the same ground truth used for the importer above — see the full derivation in `.work/fix-wordchart-half-pass-splitting-plan.md`). Actual root cause, much narrower: `wordChart.js`'s `interleaveCells(peyoteRowCells(rows, 0), peyoteRowCells(rows, 1))` wrongly interleaves two *already-full* physical rows together for the "Rows 1 & 2" line (doubling that line's bead count), and every row after that prints as one full-width line when real single-drop peyote — confirmed via the same Loomerly ground truth — needs each band split into two alternating-position passes beyond the foundation row. No data/storage migration needed either way; the fix is confined to the export step. Logged as its own line in `.work/feature-requests-and-bugs.md` pointing at the full plan doc.
  - Next step: implement `.work/fix-wordchart-half-pass-splitting-plan.md` when picked up — row 0 stops being interleaved with row 1 (prints alone), rows 1..`cols-1` each split into an even-position and an odd-position line, `printView.js`'s row-label formatting reworked for the new (no longer 1:1 physical-row-to-printed-line) structure, full `wordChart.test.js` rewrite. Worth factoring shared split/combine logic with `loomerlyImport.js`'s pairing step at that point (mirror-image operations on the same data), per the plan's own note.

- Status as of two sessions ago: **Loomerly PDF pattern import — planning only, no code written yet.** User wants to bring existing designs into this app from Loomerly (a prior/other app) via Loomerly's PDF pattern export (word chart + color list + header stats, not an image — real extractable text, not OCR). Backlog item ("ability to import the prior app's exports," Later/optional section) now fully scoped in `.work/feature-loomerly-import-plan.md`, built from one real sample PDF the user provided (a shaped/tapered odd-count peyote strip, 19w × 80h, Rocaille 11/0, 5 colors). Not yet approved for implementation — next session should build it.
  - The plan's core finding, worth knowing before touching this: **Loomerly's "row" in its word chart is half of one of this app's own physical rows.** This app's own peyote model (`peyoteRowCells`) treats one physical row (`col` value) as up to `rows`-wide by itself, with only the *very first pair* (rows 1&2) specially combined in its own exporter. Loomerly's word chart never exceeds ~half the stated Width on any single line, even deep in a pattern's solid body — but *every* consecutive odd/even pair of Loomerly rows sums to the full Width there (confirmed on the sample: `(9)D`+`(10)D` = 19 = Width, repeatedly). So importing means internal `cols` = Loomerly's `Height` field directly (not the literal word-chart row count, which is `2×Height`), reconstructed by interleaving *every* consecutive pair of Loomerly rows into one of this app's physical rows — generalizing this app's own rows-1&2 interleave logic to every pair, not just the first. Cross-checked against the PDF's own stated finished size using this app's real (if provisional) Rocaille bead dimensions and `generatePeyoteGrid`'s formula — the pairing hypothesis predicts ~1.05"×6.34" against a stated 1.2"×6.6" (close, given the provisional bead-height gap already flagged in this file's Bead Specs section); the non-paired alternative predicts ~12.6" tall, nowhere close. See the plan's "The hard part" section for the full derivation.
  - Two sub-questions the plan flags as still needing empirical confirmation before the real parser is trusted: how a shaped/tapered row-pair (fewer beads than full width, at the pattern's tapered ends) is horizontally aligned within the row (centering is the working hypothesis, matching the sample's symmetric point shape), and the interleave order when a pair's two halves have unequal length (shaping rows). The plan's recommended first build step is a disposable geometry-validation script against a real sample before writing any of the real pure functions.
  - **Same session, follow-up 1**: the user provided two more real samples (a solid 30×40 rectangle, one plain white and one a 3-color tessellation) — both used to re-check the plan's core hypothesis, with much stronger results than the first sample allowed. Every non-combined word-chart line in both files is exactly half the stated Width (15 of 30) for the *entire* file with no exceptions, `Row 1&2` combined is exactly the full Width (30), and `rows × cols` (30×40=1,200) matches the stated `Total beads: 1,200` **exactly** (not just approximately, since a solid rectangle has no empty cells to obscure an arithmetic error) — meaningfully stronger confirmation than the original finished-size cross-check alone. The colored variant additionally validates multi-color run-list parsing on the same confirmed geometry. New finding from these two: the color list's `"Miyuki [Type] 11/0 (M)"` bead-type header line is **not always present** (both rectangles' color-list pages lack it, unlike the first sample) — the plan's bead-type detection was revised from a hard requirement on that line to a guess-and-confirm approach (prefer the header line when present, otherwise pick whichever bead type's predicted finished size is closer to the PDF's stated size, always shown to the user as an overridable choice, never silent).
  - **Same session, follow-up 2**: the user provided a fourth sample — a multi-taper 4-color Delica lattice/tessellation piece (55×89, genuine interior negative space, not a solid rectangle). This resolved the interior-gap half of the remaining open question and narrowed the rest: **interior blanks print as their own literal `-` run** (e.g. `(1)C, (1)-, (3)C`), sitting inline with real color runs — new information the first sample never revealed (it only ever had short edge totals, no interior gaps), and it removes interior-gap guessing entirely. What's left is only the *unprinted* padding beyond a row's first/last printed run when its total falls short of that row's max capacity (the tapered-tip case) — still not pixel-exact-confirmed, but now better supported than before: the picture is unambiguously mirror-symmetric, and paired rows are short by the *same absolute amount* from their own max capacities (e.g. one row 27−20=7 short, its pair 28−21=7 short) — exactly what a symmetric center-trim produces. Centering is now a well-supported default rather than a bare guess. Bead-type header was present again on this sample (`Miyuki Delica 11/0 (DB)`), reinforcing "prefer the header when present, fall back to the size-match guess when it's not" as the right design rather than an either/or.
  - Also decided: color codes never auto-resolve to hex (Loomerly's color list gives a Miyuki catalog number/name, not a hex, and this app has no catalog→hex source — same gap as this file's Bead Specs section) — import always pauses on a color-assignment step reusing Phase 8's existing native-color-picker `customColorStore` flow, one hex choice per unique code. PDF text extraction needs vendoring pdf.js (Mozilla) as static files under `/vendor/pdfjs/` — this project's first real third-party dependency (flagged in the plan as a deliberate, narrow exception to Decision #2, which was about UI frameworks specifically), dynamically imported only when Import is actually used, no CDN dependency so it stays offline-safe for iPad home-screen use.
  - Sample coverage is now considered good (four real files spanning solid/tapered/lattice, single- and multi-color) — the plan's "gather more samples" open item was downgraded to "save the actual PDF files into `.work/samples/`" (everything checked this session was against pasted document content, not real files on disk yet, and the Playwright verification step needs real files).
  - Next step: implement per the plan's build order — geometry spike (now well-armed with four real samples to check against) → `loomerlyParser.js`/`loomerlyImport.js` (pure, `node:test`-covered, including a round-trip regression test against this app's own `buildWordChart` output) → vendor pdf.js + `pdfText.js` → `importDialog.js` + library wiring → full Playwright pass on real samples → real iPad pass (untested file-picker territory — Phase 4's precedent was an image picker for Photo Trace, not a PDF).

- Status as of two sessions ago: **Print/export word chart bug — implemented in full, per `.work/fix-print-row-col-label-plan.md`.** All three pieces from the plan built and verified:
  1. **Axis fix.** `src/grid/peyote.js` gained `peyoteRowCount(cols)` (returns `cols`, the physical-row count) and `peyoteRowCells(rows, physicalRowIndex)` (every `{row, col}` cell along one physical row, `col` fixed) — the stitch-type-agnostic accessors the plan specified, so `wordChart.js` never has to know peyote's `row`/`col` are inverted relative to the row=horizontal/col=vertical convention. `src/export/wordChart.js`'s `buildWordChart` was fully rewritten to consume them instead of hand-rolling row-outer/col-inner loop nesting — this is the actual bug fix, grouping cells along the correct (offset/zigzag) axis for the first time since Phase 5.
  2. **Rows 1 & 2 combined.** `buildWordChart` now special-cases physical rows 0 & 1 (1-indexed "Rows 1 & 2"): a new `interleaveCells` helper alternates their beads bead-by-bead (`row1[0], row2[0], row1[1], row2[1], ...`) into one run-encoded entry with `combined: true, rowNumbers: [1, 2]`; every row after that is its own normal entry. Return shape changed from `{rowIndex, runs}` to `{entryIndex, rowNumbers, combined, runs}` — `entryIndex` is the printed line's own position (not a physical row number), since direction-arrow alternation is keyed off it. `src/ui/printView.js`'s new `formatRowLabel` renders `"Rows 1 & 2 (strung together)"` vs. `"Row N"` from `combined`/`rowNumbers`.
  3. **`printStartDirection` preference.** `src/storage/preferencesStore.js`'s `DEFAULT_PREFERENCES` gained `printStartDirection: 'right'`. `wordChart.js` gained `isRowReversed(chartRow, startsReversed = false)` (XOR of `entryIndex` parity against the preference, single source of truth for both the run-order reversal and the →/← arrow) and widened `displayRuns` to take the same `startsReversed` param. `mountPrintView(appState, hooks)` now takes a `hooks` param (previously none) — reads `appState.preferences.printStartDirection`, and a new `#print-start-direction-toggle` button in `#print-toolbar` (index.html) flips it via `hooks.onPreferencesChanged({printStartDirection: ...})` (the exact same generic hook `editorView.js` already used for units/panel-collapse — `main.js`'s `handlePreferencesChanged` needed zero changes) and re-renders the chart in place, no need to close/reopen. `editorView.js`'s `handlePrintExport` passes `{ onPreferencesChanged: hooks.onPreferencesChanged }` through.
  - `node:test`: 139 passing (8 net new — `wordChart.test.js` fully rewritten per the plan's fixture scheme, `cols: 1` fixtures isolate run-collapsing coverage from combining, a dedicated interleaving-regression fixture with 3 distinct-colored physical rows catches true bead-by-bead interleaving vs. naive concatenation, plus `isRowReversed`/`displayRuns` cases for the new `startsReversed` param; `peyote.test.js` gained `peyoteRowCount`/`peyoteRowCells` regression cases).
  - Verified in headless Chromium (Playwright, local `python3 -m http.server` + a driver script computing exact cell-center screen coordinates by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`'s math directly, same approach as every prior phase): a 5-bead-wide × 9-physical-row design with 3 distinct-colored rows produced the exact expected first three printed lines (`"Rows 1 & 2 (strung together) →: 1A 1B 1A 1B 1A 1B 1A 1B 1A 1B"`, `"Row 3 ←: 5C"`, `"Row 4 →: 5 blank"`) and the expected total line count (`cols - 1`, confirming the combine actually collapses two physical rows into one printed line); toggling the new button flipped the first line's arrow and run order and shifted every subsequent line's direction, without closing the print view; the toggle persisted across closing/reopening the print view, and separately across a full app reload (confirming the write actually reached `savePreferences`, not just a local closure); the materials table and unassigned-color warning (Phase 6/7 behavior) were unaffected. No console/page errors.
  - One verification-script-only flake worth noting for future sessions, not an app bug: an early run of the driver script read `shapeEntriesLength: 0` from IndexedDB moments after painting, before immediately opening Print/Export — re-run with an explicit ~1.5s settle after painting (well past the 800ms autosave debounce) showed the expected 15 entries every time afterward. Likely just the debounce's trailing timer racing the very next scripted action rather than a real persistence bug — every other reload/persistence check in the same run (including the `printStartDirection` reload check moments later) passed cleanly.
  - Next step: no phase or standalone feature plan is currently queued. `.work/feature-requests-and-bugs.md` is a running informal to-do list, currently: a more Mac-native color picker, rotate, square stitch, and drop options — none scoped or planned yet. Also still logged there: consolidating this session's new `printStartDirection` toggle plus the existing scattered units/panel-collapsed/library-view-mode toggles into one global preferences modal (explicitly deferred out of this fix, real scope on its own). Other candidates: the real iPad pass now overdue across several recent features, or anything from CLAUDE.md's Later/optional backlog (brick/square/loom grid types, cloud sync, bead-type conversion, native app rebuild, tagging/folders, prior-app export import).

- Status as of two sessions ago: **Print/export word chart bug — planning only, no code written yet.** User reported the printed stitching instructions were wrong ("row vs col" — logged in `.work/feature-requests-and-bugs.md`). What started as a suspected text-label typo turned out to be a real logic bug plus two related, user-requested additions; all three are now fully scoped in `.work/fix-print-row-col-label-plan.md`, approved by the user, and explicitly deferred to a future session/new chat for implementation:
  1. **`src/export/wordChart.js` groups cells along the wrong axis.** A physical peyote row is offset by half a bead each step (a zigzag) — that's the fixed-`col`/varying-`row` slice of `peyoteCellOriginMm`'s coordinate space, confirmed independently via `peyoteNeighbors`' adjacency and via the editor's own (correct) "Rows"/"Cols" UI labels. `buildWordChart` has grouped by the opposite (straight, non-offset) axis since Phase 5 (commit `12b6fef`) — the Phase 5 tests themselves encode the same wrong assumption. The commit that later changed the printed label from `Row` to `Col` (`428d00a`, the on-screen Rows/Cols label-swap session) didn't cause this — it just relabeled already-wrong output with a word that happened to match the wrong axis. Fix: swap `buildWordChart`'s loop nesting, add stitch-type-agnostic `peyoteRowCount`/`peyoteRowCells` exports to `peyote.js` so `wordChart.js` (and any future brick/square/loom exporter) doesn't need to know peyote's internal `row`/`col` are inverted relative to the row=horizontal/col=vertical convention. Deliberately **not** renaming `row`/`col` throughout `peyote.js`/`cellStore.js`/the tools/`canvasRenderer.js`/`appState.rows`/`cols` — those are opaque address pairs everywhere except `wordChart.js`, and `appState.rows`/`cols` are persisted IndexedDB field names, so a true rename would need a real data migration for no behavioral gain. See the plan for the full geometric derivation and the two independent checks that confirm it.
  2. **Peyote's first two physical rows print combined, not as two separate lines** — user-requested, since rows 1 & 2 can't be worked as separate thread passes (row 2's beads lock row 1's in place, so both are strung together in one alternating sequence). Confirmed with the user: single-drop peyote is strict single-bead alternation (row1[0], row2[0], row1[1], row2[1], ...); multi-drop (2+ beads alternating per stitch) is a likely-future, unscheduled stitch variant and the interleave logic is deliberately *not* generalized for it yet. This also changes direction-arrow alternation (→/←): it's per *printed instruction*, not per physical row, so combining rows 1&2 into one instruction shifts every later row's direction by one step relative to today's naive per-row alternation — mechanically derived, then explicitly confirmed correct by the user.
  3. **New global preference, `printStartDirection` (`'right'`/`'left'`, default `'right'`)** — controls which side the first printed instruction reads from, following the exact existing `preferencesStore.js` + generic `onPreferencesChanged` hook pattern already used for `units`/`panelCollapsed`/`libraryViewMode` (zero new `main.js` code needed, confirmed by reading `handlePreferencesChanged` directly — it's already a generic patch-merge-and-save). Surfaced as a toggle button in `#print-toolbar`; `mountPrintView` gains a `hooks` param it didn't need before.
  - **Deferred, logged separately** (not part of this fix): the user's idea of consolidating this new toggle plus the existing scattered units/panel-collapsed/library-view-mode toggles into one global preferences modal — real scope on its own, logged as a new line in `.work/feature-requests-and-bugs.md` for its own future plan.
  - Next step: implement `.work/fix-print-row-col-label-plan.md` in full (axis fix + `peyoteRowCount`/`peyoteRowCells` + rows-1&2 combining + `printStartDirection` preference + full `wordChart.test.js` rewrite + new `peyote.test.js` coverage + Playwright verification per the plan's "Verification" section) — user has explicitly asked for this to happen in a new chat, not the one that produced the plan.

- Status as of two sessions ago: **Draggable, confirmable paste placement + front/behind mode complete** (standalone feature, not a numbered phase — Phase 8 remains the last scheduled phase). Built per `.work/feature-paste-drag-placement-plan.md`'s build order and decisions, exactly as planned with one deliberate deviation (see below):
  - `src/tools/cutCopyTool.js`'s `applyPaste` gained a trailing `mode = 'front'` param — `'front'` (default) overwrites an already-occupied target cell exactly as before (every existing call site/test unaffected); `'behind'` skips an already-occupied target (existing bead wins) and only fills genuinely empty cells within the pasted footprint, and a skipped cell is omitted from the returned patch entirely (not even a same-value no-op entry), so an undo of a `behind` paste can't accidentally look like it touched a cell it never wrote. New `cutCopyTool.test.js` cases cover both modes, including the omitted-from-patch behavior.
  - `src/state/appState.js` gained `pasteMode: 'front'` (session-only, not persisted with the design — matches `appState.tool`) and `pastePreview: null` (`{ anchorRow, anchorCol }` while a pending paste is being positioned, or `null`).
  - `src/render/pastePreviewOverlay.js` (new) — `drawPastePreviewOverlay`, ghost-renders the clipboard's content translucently at the pending anchor plus a dashed bounding-box outline, reusing `peyoteCellOriginMm`/`worldToScreen` directly like `selectionOverlay.js` already does. No `node:test` coverage (canvas-dependent, same precedent as `selectionOverlay.js`).
  - `src/interaction/pointerRouter.js` — `'paste'` removed from `DISCRETE_TOOLS` (now just `{'fill', 'replace'}`) since paste is no longer a one-shot tap-to-stamp action; new `startPastePreviewDrag`/`continuePastePreviewDrag` (structurally identical to the existing marquee-selection drag) snap the preview's anchor directly to whichever cell is under the pointer on every move — not a relative pixel-delta drag, consistent with how fill/replace already hit-test directly. New `pasteDrag` drag-tracking state follows the same lifecycle as `selectionDrag`/`photoDrag` (cleared on pointer-end and on the second-finger-mid-gesture abort branch); the preview itself (`appState.pastePreview`) is untouched by ending the drag, exactly like ending a selection drag doesn't clear the selection. **One deliberate deviation from the plan's literal text**: the plan listed a `getPastePreview` hook alongside `onPastePreviewChange`, but nothing in the drag functions ever reads it back (the anchor always comes fresh from the current hit-test, never from previously-stored state) — added it briefly, found it dead, and dropped it from both `pointerRouter.js` and `editorView.js`'s `attachPointerRouter` call rather than ship an unused parameter.
  - `src/ui/editorView.js` — Paste (`#selection-paste`) is now a real persistent tool-state button (`aria-pressed` reflects `appState.tool === 'paste'`, alongside Select/Move Photo) rather than a fire-and-forget action. Clicking it seeds `appState.pastePreview` via new `defaultPasteAnchor()` (selection's own top-left if one's active — reproduces the old "paste in place" behavior as the starting position — otherwise the grid cell nearest the viewport's center) and switches into the `paste` tool. New `#paste-controls` group (In Front / Behind toggle, Cancel, Confirm) shows only while `tool === 'paste'` (`updatePasteControls()`, called from `setTool()` alongside the existing `updateToolButtons()`). `handlePasteConfirm()` stamps the preview into `appState.cells` via `applyPaste(..., appState.pasteMode)` as one undo-able patch. **Superseded same session, see the Confirm-exits-paste-mode follow-up below** — this originally left `pastePreview`/`tool` untouched so Confirm → drag → Confirm again could stamp repeatedly without re-clicking Paste; the user's own usage pass preferred Confirm as a one-and-done action instead, so that's current behavior now, not this. `handlePasteCancel()` clears the preview and reverts to Draw; wired to both the Cancel button and `Escape` (`handleKeyDown`'s Escape branch now checks `pastePreview` before falling through to the existing deselect behavior — the more specific in-progress action cancels first). `regenerateGrid()`/`applyResize()` both clear `pastePreview` and drop back to Draw if `paste` was the active tool, alongside their existing `selection = null` line, since resize/regenerate coordinates are meaningless against the new geometry either way.
  - `index.html`/`style.css` — `#paste-controls` added to `#tool-rail` immediately after `#selection-controls`, styled with the same compact grouped-button/`aria-pressed` convention as `#tool-toggle`/`#selection-controls`.
  - `main.js` — `openDesign()` resets `appState.pastePreview = null` alongside the existing `appState.selection = null` line, since a previous design's preview coordinates don't apply to a newly-opened one.
  - `node:test` coverage: 125 tests passing (2 new — the `applyPaste` `'behind'`/`'front'` cases in `cutCopyTool.test.js`; every other suite unchanged, since this feature touched no other pure/tested module).
  - Verified in headless Chromium (Playwright, local `python3 -m http.server` + a driver script computing exact cell-center screen coordinates by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`/`worldToScreen`'s math directly in the script, same approach as every prior phase): Copy → Paste shows the ghost immediately at the selection's own position with Confirm enabled; dragging visibly relocates the ghost (pixel-sampled at the old and new positions — canvas alpha is always opaque since the background fill is drawn first, so translucency was confirmed via color *blending* against the background, not a reduced alpha channel); Behind mode over a footprint with a pre-existing bead and an empty cell left the pre-existing bead's color exactly untouched and filled only the empty cell, both pixel-sampled with the ghost dragged well clear first (sampling *through* the still-rendered ghost — since Confirm doesn't clear it — would otherwise read a blended color rather than the true committed one, an early false-negative in this same verification pass); Front mode over a fresh overlap **did** overwrite the pre-existing bead, confirmed against a clean pre-paste sample taken before any ghost ever sat on top of it; an Undo immediately after a confirmed Behind-paste restored the pre-paste canvas exactly (full-canvas-position pixel check) and Redo reapplied it; two Confirms in a row without re-clicking Paste both landed as independent stamps with the tool never leaving `paste` in between; Cancel (button, then separately Escape) hid `#paste-controls`, reverted to Draw, left both previously-committed stamps pixel-identical, and reverted the ghost's last (empty) footprint back to plain background; Escape with no active paste preview still deselected an active marquee selection (regression check against the existing Phase 7 behavior); triggering a resize while a paste preview was active hid `#paste-controls` and reverted to Draw with no console/page errors. No errors across the full run.
  - **Not yet done**: real iPad pass (drag-to-position feel for the ghost vs. the existing marquee-selection/move-photo drags it's modeled on; front/behind toggle and Confirm/Cancel button touch-target size in the already fairly full `#tool-rail`). Both of the plan's "open, low-stakes" implementation calls were taken as-planned and left open: direct top-left snapping (vs. a relative-offset or center-anchored drag) and no keyboard shortcut for Confirm (only Escape-to-cancel).
  - **Follow-up tweaks, same session, from the user's first real usage pass:**
    - **Edge auto-pan during select/paste drags.** The user reported being able to extend a marquee/paste-preview drag rightward but getting "stuck" leftward once the pointer ran out of room — a real limitation of the mouse/finger's own travel distance once the visible canvas doesn't show the whole grid (e.g. zoomed in), not a grid-math bug. `pointerRouter.js` gained a self-perpetuating `requestAnimationFrame` loop (`startEdgePanLoop`/`stopEdgePanLoop`/`tickEdgePan`) tied to a `selectionDrag`/`pasteDrag`'s lifetime: while either is active, every frame it re-reads the pointer's last known canvas-local position (`pointers.get`, already tracked) and pans `viewport.originXmm/originYmm` proportionally to how deep into a 48px edge margin (`EDGE_PAN_MARGIN_PX`) the pointer sits, capped at `EDGE_PAN_MAX_SPEED_PX_PER_FRAME = 14`px/frame so a pointer that's moved way off-canvas (pointer capture keeps delivering its events) doesn't accelerate without bound. Deliberately keeps panning even while the pointer holds perfectly still at the edge — driven by the rAF loop's own re-reads, not by waiting for more `pointermove` events, which is the actual fix (a stationary-at-the-edge pointer generates no further move events to react to). Started from `startSelectionDrag`/`startPastePreviewDrag`, stopped explicitly on pointer-end and the existing second-finger-abort branch (the loop is also self-terminating the frame after either drag var goes null, as a backstop). Scoped to select/paste only, per the user's report — draw/erase/photo-move were untouched.
    - **Paste ghost opacity bumped to 90%.** `pastePreviewOverlay.js`'s `PASTE_PREVIEW_ALPHA` raised from `0.55` to `0.9` — the user found the original translucency made it hard to judge the pasted content's actual color before confirming. One-line change, no other logic affected (Front/Behind's own blending semantics are unrelated to render alpha).
    - **Click-outside-selection deselects.** A plain tap (`pointerdown` → `pointerup` with no movement) landing outside whatever marquee selection was already active when the tap started now clears the selection, instead of leaving the stray 1-cell box `startSelectionDrag` shows for live drag feedback. `pointerRouter.js`'s `startSelectionDrag` now takes a `getSelection` hook (new, alongside `getClipboard`/`getPhotoTrace`) to capture `tapOutsideExisting` at drag-start time, and `continueSelectionDrag` marks `selectionDrag.moved = true` the first time the hit cell actually changes; `handlePointerEnd` resolves it — if the gesture never moved and started outside the existing selection, fires `onSelectionChange(null)` instead of leaving the drag's final 1-cell value. A tap *inside* the existing selection, or any tap when nothing was previously selected, is unaffected (same behavior as before — collapses to/starts a 1-cell box, since only the "was outside and never moved" case changed). `editorView.js` wires the new `getSelection: () => appState.selection` hook into `attachPointerRouter`.
    - Regression: all three verified in headless Chromium (a sustained edge-hold during a select drag measurably panned the viewport with the pointer stationary the whole time, confirmed by a painted bead disappearing from a fixed on-screen point with no further pointer movement after reaching the edge; the paste ghost's reddest non-border pixel now reads deeply saturated, matching a ~90%-alpha blend rather than the old ~55%; a plain click outside an active selection disabled Copy/Cut, a fresh drag afterward still created a new selection normally, and a tap inside an active selection left it untouched), plus the full `node:test` suite (125 passing, unchanged — none of these three touched a pure/tested module).
    - **Confirm now exits paste mode entirely, superseding the repeated-stamping design above.** Asked directly via `AskUserQuestion` since it's a real behavior tradeoff, not a bug fix (repeated stamping without re-clicking Paste was a deliberate feature of the original build, confirmed by the user's own prior review): the user chose "one-and-done" over keeping repeat-stamp. `editorView.js`'s `handlePasteConfirm()` now additionally sets `appState.pastePreview = null`, `appState.selection = null` (clears the leftover marquee box from the original Copy — it shouldn't linger once its content's already been placed), calls `updateSelectionButtons()`, and `setTool('draw')`, all after the `applyPaste`/`pushPatch` stamp. To stamp a second copy the user now clicks Paste again (which still seeds the preview from `defaultPasteAnchor()` as before). Verified in headless Chromium: Confirm hides `#paste-controls`, reverts to Draw, disables Copy/Cut/Deselect (selection genuinely cleared, not just visually), the stamped content lands correctly, and Undo still reverts it as one step — plus the full `node:test` suite (125 passing, unchanged, no pure module touched).
    - **Paste anchor was still bound to the grid's own left/top edges even after the edge-auto-pan fix above.** The user reported this as a separate, still-present limitation: `clampedHit`'s clamp of row into `[0, rows)` and col into `[0, cols)` (shared by both selection-drag and paste-preview-drag) meant the paste anchor itself could never go negative — so unlike the right/bottom edges (where the anchor can sit near `rows-1`/`cols-1` while the rest of the footprint still overhangs past the boundary unclamped), the top-left corner of a pasted footprint could never be dragged past row 0 / col 0 to make it hang off *that* side. Selection correctly stays clamped (it can only ever mark cells that already exist); paste's clipboard content has no such constraint. Added `peyoteCellAtPointUnbounded(xMm, yMm, beadWidthMm, beadHeightMm)` to `peyote.js` — same hit-test formula as `peyoteCellAtPoint`/`peyoteCellAtPointClamped` but with no bounds restriction at all (row/col can come back negative or past rows/cols); `pointerRouter.js` gained a parallel `unboundedHit()` (mirroring `clampedHit()`) used only by `startPastePreviewDrag`/`continuePastePreviewDrag`, leaving `clampedHit()`/selection untouched. `applyPaste` already clipped whatever ends up off-grid at Confirm time (Phase 7), so nothing downstream needed to change — only the hit-test itself was the constraint.
      - **Pre-existing latent bug found and fixed in the same pass**: `peyoteCellOriginMm` (and the new `peyoteCellAtPointUnbounded`) compute the odd/even row stagger via `row % 2 === 1`, but JS's `%` keeps the sign of its left operand (`-1 % 2 === -1`, not `1`), which would have silently *skipped* the stagger offset for every negative odd row. This was never reachable before this session — `peyoteCellOriginMm` had only ever been called with `row` already inside `[0, rows)` — but the paste ghost overlay (`pastePreviewOverlay.js`) now calls it with genuinely negative rows via the fix above, so the bug would have shown up as a visibly misaligned stagger on the ghost the moment it was dragged off the left edge. Fixed by normalizing through a `positiveMod2(n)` helper (`((n % 2) + 2) % 2`) in both functions; `peyoteCellAtPoint`/`peyoteCellAtPointClamped` didn't need the same fix since both already clamp/bounds-check `row` into non-negative range *before* computing the offset.
      - `node:test` coverage: 131 passing (6 new — `peyoteCellOriginMm`'s negative-odd/negative-even row cases; `peyoteCellAtPointUnbounded`'s round-trip, negative-row, past-bounds, and negative-parity cases, all built via `peyoteCellOriginMm` + a half-cell nudge rather than exact-multiple coordinates, which turned out to be floating-point-fragile at exact cell boundaries and cost a round of test debugging before landing on the same "nudge into the cell interior" convention this test file already used elsewhere).
      - Verified in headless Chromium with precise grid-math-derived click coordinates (same reimplementation approach as every prior verification script): painted a red/blue vertical pair, copied it, dragged the paste anchor to row -1 — confirmed the blue (row-offset-1) cell landed on-grid at row 0 while nothing rendered off-grid at row -1 (the red, row-offset-0 cell stayed correctly clipped, proving the anchor genuinely reached -1 rather than clamping to 0, which would have shown red at row 0 instead); repeated the equivalent check for the top edge with a horizontal red/blue pair dragged to col -1, confirming blue landed at col 0. No console/page errors; full `node:test` suite unaffected.
  - Next step: no phase or standalone feature plan is currently queued. `.work/feature-requests-and-bugs.md` is a running informal to-do list (not a formal plan doc like the others in `.work/`), currently: a more Mac-native color picker, a print-instructions row/col bug, rotate, square stitch, and drop options — none scoped or planned yet. Other candidates: the real iPad pass now overdue across several recent features (this one included), or anything from CLAUDE.md's Later/optional backlog (brick/square/loom grid types, cloud sync, bead-type conversion, native app rebuild, tagging/folders, prior-app export import).

- Status as of two sessions ago: **Library thumbnail gallery feature complete** (Later/optional backlog item, standalone — not a numbered phase; Phase 8 remains the last scheduled phase). Built per `.work/feature-library-thumbnails-plan.md`'s build order:
  - `src/palette/colorLibrary.js` gained `resolveSwatchHex(customColors, colorId)`, deduping the colorId→hex lookup previously copy-pasted in `editorView.js`'s `resolveColor`; that function is now a one-line wrapper around it. `printView.js`'s `resolveSwatch` (needs the whole swatch object, for `name` too) was deliberately left as its own thing, per the plan.
  - `src/render/thumbnailRenderer.js` (new) — pure-ish `renderThumbnailDataUrl(gridParams, cells, resolveColor, beadShape, maxSizePx)`, draws only occupied cells (no outlines/empty-cell dots — noise at thumbnail scale) into a detached `<canvas>` fit to `maxSizePx` on its longer side, returns a PNG data URL. An empty design still returns a valid blank-white data URL rather than `null` — that distinction is what separates "thumbnail of an empty-but-saved design" from "never saved under this feature."
  - `src/storage/designStore.js`'s `createDesign` seeds `thumbnailDataUrl: null`; `duplicateDesign`'s existing `...original` spread already carries it over correctly. `src/storage/preferencesStore.js`'s `DEFAULT_PREFERENCES` gained `libraryViewMode: 'list'`.
  - `main.js`: `persistCurrentDesign()` now regenerates `thumbnailDataUrl` from live `appState` on every save (no dirty-tracking — same philosophy as the rest of that function) via `renderThumbnailDataUrl(appState.gridParams, appState.cells, (colorId) => resolveSwatchHex(appState.customColors, colorId), BEAD_TYPES[appState.beadTypeKey].shape, 200)`, falling back to whatever was already stored on the rare chance `gridParams` isn't derived yet. New `handleViewModeChanged(mode)` persists `libraryViewMode` the same way `handlePreferencesChanged` does; `boot()` calls `libraryController.setViewMode(...)` with the stored preference (coerced to `'list'` unless exactly `'gallery'`, so an old preferences row missing the field doesn't break the toggle) before the first `renderList`.
  - `src/ui/libraryView.js`: `buildRow` inserts a `.library-row-thumb` div (an `<img>` if `design.thumbnailDataUrl` is set, otherwise empty — styled as a neutral placeholder box, no broken-image risk) between the drag handle and the info button. New `setViewMode(mode)` toggles `#library-list`'s `gallery-mode` class and both toggle buttons' `aria-pressed`, without re-rendering rows (list/gallery share identical row markup, switched by CSS only, per the plan's decision). Drag-reorder's target-detection gained a mode-aware `isBeforeTarget` helper — list mode's Y-only comparison is untouched (same expression, just routed through the helper); gallery mode additionally compares X when the pointer's Y falls within a tile's own row band, falling back to Y-only across a row-wrap boundary.
  - `index.html`/`style.css` — `#library-view-toggle` (List/Gallery buttons) added to `#library-header`; `.library-row-thumb` (44px box, `object-fit: contain`) added to the list row; `#library-list.gallery-mode` switches the container to a wrapping grid (`repeat(auto-fill, minmax(9rem, 1fr))`) and `.gallery-mode .library-row`/`.library-row-thumb`/`.library-drag-handle`/`.library-row-action` turn the same row markup into a vertical card (drag handle repositioned to an absolute corner overlay, since a compact tile has no spare horizontal strip for it).
  - No `node:test` coverage added — `thumbnailRenderer.js` touches the DOM (`document.createElement('canvas')`), same precedent as `canvasRenderer.js`/`selectionOverlay.js` having none; `designStore.js`/`preferencesStore.js`/`libraryView.js` already had none (no IndexedDB/DOM in Node).
  - Verified in headless Chromium (Playwright, local `python3 -m http.server` + driver scripts, same approach as every prior phase, colors painted via computed screen coordinates reimplementing the app's own grid/viewport math in-page): three designs painted with distinct colors produced three non-null, pixel-verified-correct, mutually distinct `thumbnailDataUrl`s (read directly from IndexedDB); a design saved without ever being drawn in got a real blank-white thumbnail (not a placeholder) — confirming the plan's documented "any save produces a real thumbnail, empty or not" behavior; a genuinely legacy-shaped record seeded directly into IndexedDB with no `thumbnailDataUrl` field at all showed the placeholder box with no `<img>`, confirming that's the actual trigger for the placeholder rather than an empty pattern. List→Gallery toggle switched layout and `aria-pressed` correctly and persisted across a reload; toggling back to List worked. List-mode drag-reorder produced the exact expected id order (regression-checked against Phase 4's existing scenario). Gallery-mode drag-reorder (6 tiles at a narrowed 500px viewport, confirmed wrapping to 2 rows) correctly handled both a drag from the second row to before the first tile in the first row, and a drag within the same row past a neighbor. `renderThumbnailDataUrl` timed at ~12ms against a 300×200/5,000-cell dense pattern (reusing other phases' perf fixture), well under the 800ms autosave debounce window.
  - **Not yet done**: real iPad pass (thumbnail render legibility on Retina, gallery tile touch-target/drag feel, the 200px render size and 9rem tile minimum are both starting guesses per the plan's "open, low-stakes" list); no backfill for designs saved before this feature shipped (deliberate — see plan's Decisions; they'll get a real thumbnail the next time they're opened and saved, same tradeoff `migrateDesign.js` made for Phase 6).
  - Next step: no phase or standalone feature plan is currently queued. Candidates: the other standalone plan already on disk (`.work/feature-paste-drag-placement-plan.md`, not yet implemented), the real iPad pass overdue across several recent features, or anything from CLAUDE.md's Later/optional backlog (brick/square/loom grid types, cloud sync, bead-type conversion, native app rebuild, tagging/folders, prior-app export import).

- Status as of two sessions ago: **Phase 8 (side panel layout + custom color palettes) complete.** Built per `.work/phase-8-implementation-plan.md`'s build order, both parts:
  - **Part A — layout restructuring.** `index.html`'s `#editor-view` changed from one wrapping `#controls` bar + a `#color-palette` bar above the canvas into `#top-bar` (back/bead-type/rows/cols/resize/units/size-readout/reset-view/history/print-export/a new `#panel-toggle`) spanning the full width, with `#editor-body` below it holding three regions: `#tool-rail` (draw/erase/fill/replace/select, selection-controls, Clear — reflowed into a vertical column, `#clear-pattern` pinned to the rail's bottom via `margin-top: auto`), `#pattern-canvas` (unchanged, now `flex: 1; min-width: 0` so it absorbs whatever width the rail/panel don't take), and `#side-panel` (new — `palette-section`/`colorway-section`/`photo-trace-section`, each a `<section>` with its own `<h2>`). Every pre-existing element **id** was preserved, so `editorView.js`'s existing `document.getElementById` calls needed no changes — only the surrounding containers moved. `style.css` replaced `#controls`'s rules with `#top-bar`/`#editor-body`/`#tool-rail`/`#side-panel` equivalents, reusing the `[hidden] { display: none !important }` global rule from Phase 7's cascade-bug fix for the panel's collapse/expand (no new `.collapsed` class needed). `#panel-toggle` toggles `#side-panel`'s `hidden` attribute; `preferencesStore.js`'s `DEFAULT_PREFERENCES` gained `panelCollapsed: false` and `editorView.js`'s `handlePanelToggle` writes it back via the existing `onPreferencesChanged` hook (prior-app pain point #1 — this persists across designs, not per-session). Collapsing doesn't fire `window.resize`, so `handlePanelToggle` calls `scheduleRedraw()` explicitly (`render()` already calls `resizeCanvasForDisplay` on every redraw). On mount, `sidePanel.hidden` is set from `appState.preferences.panelCollapsed` *before* the canvas's first `resizeCanvasForDisplay` measurement, since the panel's width affects the canvas's available width.
  - **Part B — custom color palettes.** `src/palette/colorLibrary.js` lost `PLACEHOLDER_SWATCHES`/`COLOR_LIBRARIES` entirely (kept only `UNASSIGNED_SWATCH`, an independent colorway concept, not catalog data) — the placeholder array was explicitly documented since Phase 2 as "a data-only change" to swap out, and this is that swap, to user-entered data instead of real catalog data (sourcing actual Miyuki DB/RR numbers remains impractical — 900+ SKUs each, no machine-readable hex source, per the Bead Specs gap). `db.js` bumped to `DB_VERSION = 3`, adds a `customColors` store (keyPath `id`). `src/storage/customColorStore.js` (new) — `listCustomColorsSorted`/`createCustomColor`/`saveCustomColor`/`deleteCustomColor`, mirroring `designStore.js`'s CRUD shape exactly; colors are scoped per `beadTypeKey` (delica11/rocaille11 get fully independent lists from the start, not an accidentally-shared array — see this file's updated Bead-type-conversion backlog note below). Reorder reuses `designOrder.js`'s existing `orderForInsertAt` directly (already axis-agnostic). `appState.js` gained `customColors: []` (current bead type's list only, refreshed on design open and bead-type change) and changed `selectedColorId`'s default from a static first-swatch lookup to `null` (a real "nothing picked yet" state now that there's no static catalog to point to — distinct from, but reuses the rendering of, Phase 6's "unassigned cell" `null`). `editorView.js`: `resolveColor`/`renderColorPalette` now read `appState.customColors` instead of `COLOR_LIBRARIES[beadTypeKey]`; a zero-color bead type shows an empty-state message ("No colors yet — tap + to add one") instead of an empty swatch row; a dashed `+` tile is appended to the swatch grid (`handleAddColorClick` → `colorPickerInput.click()` → the native `<input type="color">`'s `change` event → `window.prompt('Name this color')`, same UX convention as colorway rename — cancelling or blanking the name aborts without creating anything) and routes through a new `hooks.onCustomColorAdded`. A **Manage** button (`#color-manage-toggle`) swaps the swatch grid for a `#color-manage-list` in the same screen real estate (not both open at once) — drag-reorder/rename/delete rows, structurally identical to `libraryView.js`'s design-list rows including the same pointer-capture-on-the-list-container-not-the-row fix Phase 4 discovered (capturing the dragged row itself breaks the instant `insertBefore` reparents it mid-gesture). `handleBeadTypeChange` became `async`, now awaiting a new `hooks.onBeadTypeChanged(beadTypeKey)` before calling `regenerateGrid()`, so the palette `regenerateGrid()` renders is already the new bead type's own list, not a stale one. `main.js` added the five new hooks (`onBeadTypeChanged`/`onCustomColorAdded`/`onCustomColorRenamed`/`onCustomColorDeleted`/`onCustomColorReordered`), each mutating `appState.customColors` in place after the store write (same pattern `handlePreferencesChanged` already uses for `appState.preferences`); `openDesign()` became `async` and now awaits `listCustomColorsSorted(appState.db, design.beadTypeKey)` before mounting the editor (small/fast rows, unlike Phase 7's multi-MB photo-trace blob, so no need for that phase's async-after-mount pattern — awaited synchronously instead) — its two callers (`handleOpen`, `handleCreate`) now `await openDesign(...)`. `src/ui/printView.js`'s materials-table swatch/name lookup switched from a `COLOR_LIBRARIES[beadTypeKey]` closure to reading `appState.customColors` directly (passed through `mountPrintView`'s existing single read of `appState`), needing no new hook since the print overlay is already read-only/ephemeral.
  - `node:test` coverage: all 123 existing tests pass unchanged — this phase touched no pure/tested module (grid math, tools, history, colorway sync, resize, word chart, color codes were all untouched); `customColorStore.js` has no `node:test` coverage for the same reason `designStore.js` already didn't (no IndexedDB in Node) — verified in headless Chromium instead, consistent with every other storage-layer module.
  - Verified in headless Chromium (Playwright, local `python3 -m http.server` + two driver scripts): layout — top bar/tool rail/side panel all visible and correctly ordered left-to-right after creating a new design; panel-toggle collapses/re-expands `#side-panel` and the canvas measurably widens when collapsed (bounding-rect width compared, not just the DOM attribute); every pre-Phase-8 toolbar control (tool selection, colorway-new, undo/redo, print/export) still fires correctly through the reflowed containers. Custom colors — a brand-new bead type shows the "No colors yet" empty state; adding a color via the `+` tile → native color input → prompt flow shows exactly one swatch carrying the entered name, and painting with it produces the exact chosen hex on canvas (pixel-sampled at a screen point computed by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`/`worldToScreen`'s math in-page via dynamic `import()` of the app's own modules, same approach prior phases' scripts used, needed here because a dead-center click on an even-dimension grid lands exactly on a cell boundary/outline rather than inside a bead); Manage mode shows both added colors and hides the swatch grid (not both at once); rename updates the manage-list label; delete removes the row and the swatch grid reflects it after leaving manage mode; switching bead type to `rocaille11` shows *its own* empty palette (no cross-contamination from `delica11`'s color), and switching back restores `delica11`'s color untouched. A reload confirmed both `panelCollapsed` and the custom color (including its renamed label) persisted correctly. A second script confirmed Print/Export's materials legend now resolves names/hex correctly from `appState.customColors` (a swatch named and colored via the picker flow shows up correctly in the printed legend table, with the swatch's computed background color matching the chosen hex exactly) and that the print overlay still opens/closes cleanly.
  - **Not yet done**: real iPad pass (rail button touch-target size and column width at the two flagged sizes — Pro 11"/mini, both orientations; side-panel usability in portrait, the tightest case per Phase 7's toolbar-crowding note; the native `<input type="color">` picker's actual on-device feel, unverified so far though it should be the system picker; panel-toggle discoverability with a text-only label, no icon yet). All four of the plan's "open, low-stakes implementation calls" were taken as-planned and left open (icon rail vs. text labels; `customColorList.js` as its own module vs. folded into `editorView.js` — folded in, since it's a small addition to a file that already inlines the structurally-similar colorway controls; palette search/filter, deferred until a list is actually large enough to need it; the `18rem` side-panel width, a starting guess not yet checked against real content at the two flagged iPad sizes). The still-unscheduled "Bead-type conversion" backlog item is directly affected by this phase — see this file's updated note under Later/optional, above.
  - Next step: **no further phase is currently defined in the Phase Plan above — Phase 8 was the last scheduled phase.** Candidates for a next session: the real iPad pass just listed (arguably overdue — several phases' iPad passes are still outstanding); a real icon set for the tool rail (Phase 8's explicit scope-boundary follow-up); or picking up something from the Later/optional section (brick/square/loom grid types, cloud sync, bead-type conversion — now with a clearer color-remapping problem statement per the note above — native app rebuild, thumbnail grid library view, tagging/folders, prior-app export import).

- Status as of two sessions ago: **Phase 7 (remaining tools) complete — fill, color-replace, cut/copy/paste, mirror, photo trace overlay.** Built per `.work/phase-7-implementation-plan.md`'s file breakdown and build order, all five features:
  - `src/grid/peyote.js` — added `peyoteNeighbors(row, col)` (the six physically-adjacent cells for peyote's offset-row structure, per the plan's derivation) and `peyoteCellAtPointClamped` (same hit-test as `peyoteCellAtPoint` but clamps into grid bounds instead of returning `null`, for marquee-drag tracking past the canvas edge).
  - `src/tools/fillTool.js` — `applyFill(cells, startRow, startCol, colorId, rows, cols)`, iterative flood fill over `peyoteNeighbors` (not naive row/col ±1 adjacency — verified with a fixture where the two diverge). Filling into absent cells is a shape change, same as `drawTool.js`, needing no special handling elsewhere.
  - `src/tools/colorReplaceTool.js` — `applyColorReplace(cells, sourceColorId, targetColorId)`, global across the whole active colorway (not selection-scoped in v1, per the plan's scope boundary).
  - `src/tools/cutCopyTool.js` — `buildClipboard`/`applyEraseRegion`/`applyPaste`, sharing one clipboard shape (`{rows, cols, cells: [[relRow, relCol, colorId], ...]}`, relative coordinates so it survives a design/colorway/resize switch unmodified — see below).
  - `src/tools/mirrorTool.js` — `applyMirror(cells, selection, axis)`, a read-all-then-write-all swap within the selection's bounding box. Vertical mirror is only correct on an odd-height selection (reversing row order flips every row's physical stagger parity when the height is even — a structural property of the offset-row grid, not a bug) — enforced at the UI layer (button disabled with an explanatory title), not inside the function itself, since there's no way to make an even-height vertical flip *correct*, only silently wrong.
  - `src/interaction/pointerRouter.js` — extended tool dispatch: `STROKE_TOOLS` (draw/erase, unchanged continuous-drag path) vs. `DISCRETE_TOOLS` (fill/replace/paste, one action per `pointerdown` via a new `performDiscreteAction`, no interpolation) vs. `'select'` (a new `selectionDrag` marquee state machine, mirroring `mouseDrag`'s shape) vs. `'move-photo'` (a new `photoDrag` state machine that translates `appState.photoTrace` directly, plus a pinch-branch fork that scales the photo via `scalePhotoToAnchor` instead of zooming the viewport when this tool is active). All four interaction shapes share the existing `onStrokeCommitted`/`commitStroke` patch-commit path where applicable, so undo/redo needed zero changes (Phase 3's `historyStore.js` was already agnostic to how many cells one patch touches or where the array came from — the same observation the Phase 6 plan made and leaned on).
  - `src/render/selectionOverlay.js` (new) — `drawSelectionOverlay`, a dashed-rectangle marquee reusing `peyoteCellOriginMm` directly rather than needing new grid math.
  - `src/render/canvasRenderer.js` — `drawPeyoteGrid` gained an optional trailing `photoLayer` parameter (`{image, opacityPercent, xMm, yMm, widthMm, heightMm}`), drawn right after the background fill and before the cell loop so beads render on top of the reference photo; defaults to `null` so every existing call site/test is unaffected.
  - `src/state/photoTrace.js` (new) — pure `defaultPhotoPlacement` (centers a loaded image over the grid's bounding box, scaled uniformly so its larger dimension fits) and `scalePhotoToAnchor` (pinch-to-scale math, mirroring `pointerRouter.js`'s existing `zoomToAnchor` but targeting the photo's own `xMm/yMm/widthMm/heightMm` instead of the shared viewport).
  - `src/storage/db.js` — `DB_VERSION` bumped to 2, adds a `photoTraces` object store (keyPath `designId`). `src/storage/photoTraceStore.js` (new) — `getPhotoTrace`/`savePhotoTrace`/`deletePhotoTrace`. Kept as its own store rather than embedded in a design record specifically so a multi-MB reference-image `Blob` isn't re-serialized on every routine cell-edit autosave — position/opacity changes happen far less often than cell edits.
  - `src/state/appState.js` — added `selection`, `clipboard`, `photoTrace` fields, none following the shared-shape colorway model (editor-session state layered on top of `appState.cells`, not part of a design's saved shape/color data).
  - `src/ui/editorView.js` — wired Fill/Replace/Select into the existing `#tool-toggle` group (`setTool()` helper replacing the old per-tool handler duplication); new `#selection-controls` group (Copy/Cut/Paste/Mirror↔/Mirror↕) with a shared `updateSelectionButtons()` (Mirror↕ additionally disabled on an even-height selection, with an explanatory `title`); new `#photo-trace-controls` group (Load Photo via a hidden file input + `createImageBitmap`, an opacity `<input type="range">`, a Move Photo toggle, Remove Photo). One deliberate UI-behavior change from the literal plan text: the color palette's click handler previously always forced `tool = 'draw'` on any swatch pick — extended so it only does that when the current tool is already `'draw'`/`'erase'`, leaving Fill/Replace/Select/Paste/Move Photo undisturbed, since Replace in particular needs the palette purely as a target-color picker while staying on Replace (the plan's text assumed this already worked but didn't spell out the change needed). `render()` now passes `appState.photoTrace` through to `drawPeyoteGrid` and calls `drawSelectionOverlay` after. Exposes `setPhotoTrace(photoTrace)` on the mount's returned controller, for main.js's async post-mount photo load (see below). `regenerateGrid`/`applyResize` both clear `appState.selection` (coordinates meaningless against new geometry); colorway switch does not (per the plan).
  - `main.js` — `openDesign()` sets `appState.selection = null` and `appState.photoTrace = null` synchronously (so the editor mounts immediately without waiting on a possibly-multi-MB image decode), then kicks off async `loadPhotoTraceForDesign(design.id)` which decodes the stored `Blob` and calls `editorController.setPhotoTrace(...)` once ready — guarded against the user having already left the design by then. A second debounce (`debouncedPhotoSave`, targeting `photoTraceStore` via a new `persistPhotoTrace()`) runs alongside the existing cell-autosave debounce, created/flushed at the same lifecycle points (`backToLibrary`, `visibilitychange`/`pagehide`); Remove Photo goes through an immediate (non-debounced) `handlePhotoTraceRemoved()` instead, flushing any pending photo-save first to avoid a race with the delete.
  - `index.html`/`style.css` — new toolbar buttons/groups above, styled consistent with the existing compact grouped-button convention.
  - **Pre-existing bug found and fixed, unrelated to Phase 7's own scope but discovered by this phase's toolbar-layout verification screenshots:** `style.css`'s `#library-view, #editor-view { display: flex; ... }` rule (present since Phase 4) silently defeated the `hidden` attribute's hiding effect — an author-stylesheet `display` declaration beats the user-agent stylesheet's `[hidden] { display: none }` by cascade *origin*, before specificity is even considered, so both views have been computing to `display: flex` simultaneously regardless of which was supposed to be hidden, for every session since Phase 4. This was never caught visually because every prior Playwright verification pass waited on the `hidden` *attribute* (`:not([hidden])` selectors), which was always toggling correctly — never on actual computed `display`. Fixed with a global `[hidden] { display: none !important; }` rule in `style.css`; confirmed via `getComputedStyle` before/after (library/editor now correctly compute to `none`/`flex` and vice versa) and by re-running every Phase 7 Playwright check afterward with no regressions. `#print-view`'s hidden-toggle was unaffected (its CSS block never sets `display`, so the UA rule already applied). Worth noting for any future view added the same way — the `!important` rule now protects all of them going forward.
  - `node:test` coverage: 123 tests passing (29 new — `peyote.test.js`'s `peyoteNeighbors`/`peyoteCellAtPointClamped` cases including a symmetric-adjacency sweep; `fillTool.test.js` incl. the 6-connectivity-vs-4-connectivity divergence fixture and a 60,000-cell timing check; `colorReplaceTool.test.js`; `cutCopyTool.test.js`; `mirrorTool.test.js`; `photoTrace.test.js`'s placement/scale math; plus all of Phases 1–6's existing suites unchanged).
  - Verified in headless Chromium (Playwright, same local-`http.server` + exact-cell-click approach as prior phases, coordinates computed by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`/`worldToScreen` directly in the test script): fill on an enclosed region changes exactly the connected area and leaves a differently-colored pre-existing cell untouched, one undo step reverts the whole fill; color-replace changes every matching cell (including a non-contiguous pair) in one action/one undo step; a marquee selection enables Copy/Cut/Mirror↔ and Mirror↕ (only on odd-height, confirmed disabled on an even-height reselection); Copy→Paste at a different anchor reproduces a 2×2 mixed-content region exactly (including the one absent corner) while leaving the original untouched, and undo removes the whole paste stamp at once; Cut removes the original region in one step and undo restores it; vertical mirror on an odd-height selection swaps top/bottom correctly; loading a photo shows it visible at the expected default placement (sampled off-center from the cell midpoint, since dead-center on an empty cell hits the small empty-cell dot marker, which the photo — see below — now draws on top of too), raising opacity increases saturation toward the source image, Move Photo drag visibly repositions it, Remove Photo hides the photo controls; a full reload-from-library pass confirmed both the cell edits and the photo trace persisted correctly; a dedicated toolbar-layout pass across four iPad-ish viewport sizes (Pro 11" and mini, both orientations, with a photo loaded so every optional control group is visible) found the toolbar wraps to 2–3 lines and consumes 15–20% of viewport height with no horizontal overflow, leaving 900px+ of usable canvas even in the most cramped case (mini portrait) — the plan's default flat-button-group assumption held, no overflow/popover needed.
  - **Follow-up fixes, same session, from the user's first real usage pass:**
    - **Photo trace paint order reversed.** Built initially with the photo drawn *behind* the grid (occluded wherever a bead was placed, tracing-paper-style — matching the plan's literal "behind the grid" phrasing), but the user's actual expectation on first use was a translucent overlay on top of *everything*, beads included, so the reference and the beadwork-so-far stay simultaneously comparable. Confirmed via `AskUserQuestion` rather than assumed, since the two are genuinely different design intents, not a bug either way. `canvasRenderer.js`'s `drawPeyoteGrid` now draws `photoLayer` *after* the cell loop instead of before/between the background fill and the cell loop; `Decision #10`'s "background reference image" wording above is amended accordingly. Verified: a bead drawn blue (`rgb(44,62,80)`) before loading a solid-red test photo reads as clearly red-shifted (`rgb(170,24,32)`) afterward, at the default 60% opacity, confirming the photo now visibly composites over already-placed beads rather than being hidden by them.
    - **No way to deselect.** The marquee selection had no clear path back to "nothing selected" — switching tools deliberately leaves it intact (so Copy→switch-to-Paste-tool works), but that also meant there was no way to *stop* having a selection short of resize/regenerate. Added a **Deselect** button in `#selection-controls` (enabled only when a selection exists) and an `Escape` key shortcut (`editorView.js`'s `handleKeyDown`, ignored while a text `<input>` is focused, same guard the existing undo/redo shortcut already uses).
    - **No desktop-friendly photo resize.** Pinch-to-scale (two-finger touch) was the only way to resize the photo trace, wired for the iPad case; a Mac trackpad/mouse session (this project's dev environment, per User Context) had no equivalent at all. `pointerRouter.js`'s `handleWheel` now checks `getTool() === 'move-photo'` on `ctrl+wheel` (the same gesture Safari/Chrome already synthesize from a trackpad pinch) and scales the photo via `scalePhotoToAnchor` instead of zooming the viewport when that tool is active; plain `ctrl+wheel` viewport zoom is unaffected when any other tool is selected.
    - Regression: all three fixes verified in headless Chromium (Deselect via button and via Escape; Ctrl+wheel resizing the photo when Move Photo is active vs. still zooming the viewport under Draw; the full Phase 7 Playwright suite re-run afterward with no regressions), plus the full `node:test` suite (123 passing, unchanged — none of these three fixes touched a pure/tested module).
  - **Not yet done**: real iPad pass (touch feel for pinch-to-scale-photo vs. pinch-to-zoom-viewport, marquee-select drag vs. draw-stroke drag, file-picker UX for Load Photo in iPad Safari, Fill's perceived speed on a large region on real hardware, and now also confirming Deselect/Escape and the new photo-overlay-on-top layering feel right in actual use). The plan's two explicitly-deferred fast-follow items remain unscheduled: "mirror & append" (flip + place adjacent, distinct from in-place mirror since it can collide with content outside the selection) and selection-scoped color-replace (v1 always replaces across the whole active colorway). The still-unscheduled "Bead-type conversion" backlog item (Later/optional section above) is unaffected by this phase.
  - Next step: **no further phase is currently defined in the Phase Plan above — Phase 7 was the last scheduled phase.** Candidates for a next session, none yet chosen: the real iPad pass just listed; either of the two deferred fast-follow items; or picking up something from the Later/optional section (brick/square/loom grid types, cloud sync, bead-type conversion, native app rebuild).

- Status as of two sessions ago: **Phase 7 planning only — no Phase 7 code written yet.** Wrote `.work/phase-7-implementation-plan.md`, a single comprehensive plan (user's explicit choice over splitting into per-feature plan docs) covering fill, color-replace, cut/copy/paste, mirror, and the photo trace overlay — five of the six items originally listed under Phase 7. The sixth, the grid/canvas orientation toggle, was dropped from scope this session: the user confirmed it was a misunderstanding of an issue already resolved by the earlier row/col transpose fix (see the "two sessions ago" entry below), not a real remaining need — CLAUDE.md's Phase Plan section above has been trimmed accordingly.
  - Two pieces of grid/tool-logic reasoning worth knowing about going into implementation, since the plan treats both as load-bearing rather than incidental:
    - **Fill needs true peyote adjacency, not simple row/col ±1 adjacency.** Because odd rows are offset by half a bead-width (`peyoteCellOriginMm`), each cell physically touches two cells in each adjacent row (which two depends on the current row's parity), not one. The plan adds a new pure `peyoteNeighbors(row, col)` export to `src/grid/peyote.js` encoding this, with the derivation spelled out in the plan so it doesn't need to be rediscovered.
    - **Mirror Vertical only produces a correct result on an odd-height selection.** Reversing row order changes a row's parity (and therefore its physical stagger offset) whenever the selection's height is even — a structural property of the offset-row grid, not a bug to route around. The plan's `mirrorTool.js` design has the UI disable Mirror Vertical (with an explanatory title) on even-height selections rather than silently produce a warped result; Mirror Horizontal has no such constraint.
  - Other notable decisions the plan makes (see the plan doc for full reasoning): fill/color-replace/paste are single-tap "discrete" pointer actions (new `DISCRETE_TOOLS` branch in `pointerRouter.js`, reusing the existing stroke-commit path so they're each one undo step regardless of how many cells they touch); cut/copy/paste use a rectangular marquee selection (`appState.selection`, cleared on resize/regenerate, kept across colorway switches) and an in-memory-only clipboard (`appState.clipboard`, never persisted, survives design/colorway/resize since its coordinates are relative offsets); the photo trace overlay gets its own IndexedDB object store (`photoTraces`, `db.js` bumped to version 2) keyed by design id rather than being embedded in the design record, specifically so a multi-MB reference image blob isn't re-serialized on every routine cell-edit autosave; photo position/scale are stored in mm world-space so they compose with the existing viewport pan/zoom transform for free.
  - Flagged risk carried into implementation: `#controls` is already a fairly full toolbar after Phase 6; this phase adds three more tool buttons, a 5-button selection-controls group, and a photo-trace-controls group. The plan calls for a dedicated toolbar-layout check (build order step 9) before the real iPad pass, and notes the likely fix if it's too crowded is an overflow/popover, not a data-model change.
  - Next step: implement Phase 7 per `.work/phase-7-implementation-plan.md`'s build order — step 1 (`peyoteNeighbors`/`peyoteCellAtPointClamped` in `src/grid/peyote.js`) has no dependencies on anything else in the plan and is the natural starting point.

- Status as of two sessions ago: **Phase 6 (colorways) complete.** Built per `.work/phase-6-implementation-plan.md`'s file breakdown and build order — shared-shape model (Decision #8 taken literally: colorways of one pattern share one physical bead layout, only the color assigned to an occupied cell varies per colorway):
  - `src/state/colorwaySync.js` (new) — the entire shared-shape/per-colorway-color split lives here, pure: `materializeColorwayCells(shapeEntries, colorEntries)` rebuilds a colorway's `Map<cellKey,{colorId}>` (missing keys default to `{colorId: null}` — occupied but unassigned); `decomposeCellsForSave(cells)` is the inverse, splitting a materialized Map back into `shapeEntries`/`colorEntries` for persistence (a `colorId: null` entry is *omitted* from `colorEntries`, not stored as null — "missing" already means unassigned); `pruneColorwaysToShape(colorways, shapeEntries)` drops any colorway's entries that no longer exist in the shape (e.g. after an erase). Every other module keeps treating `appState.cells` as an ordinary Map — this is the only place that knows a design has more than one of those.
  - `src/palette/colorLibrary.js` — added `UNASSIGNED_SWATCH` (`{id: null, name: 'Unassigned', hex: '#d9cdf0'}`, a flat placeholder lavender distinct from every real swatch), so `editorView.js`'s `resolveColor` and rendering share one definition for "occupied, not yet colored in this colorway."
  - `src/state/resizeGrid.js` — refactored the existing per-cell remap loop (shared by `resizeCells`/`countCellsLost` since Phase 5.x) into a generic `remapEntries()` over `[key, value]` pairs; added `resizeKeyList()` (for a design's `shapeEntries`) and `resizeColorEntries()` (for each colorway's `colorEntries`) on top of it. `resizeCells`/`countCellsLost` kept their exact existing signatures/behavior — confirmed behavior-preserving via the existing test suite passing unchanged, plus new cases for the two additions.
  - `src/storage/migrateDesign.js` (new) — pure `migrateDesign(record)`: passes through any record that already has `colorways`; wraps a legacy `{cellEntries}` record (Phase 4/5-era, no `colorways` field) into a single default colorway (`shapeEntries` = the entries' keys, one colorway named "Colorway 1" holding the old colors). `src/storage/designStore.js`'s `listDesignsSorted` now runs every fetched record through this and re-saves (`Promise.all`) any that actually changed, so migration happens once per design, system-wide, the next time the library loads — not on every boot indefinitely, and not deferred until a design happens to be reopened. `createDesign` now seeds one default colorway instead of `cellEntries: []`; `duplicateDesign` migrates the source first (so duplicating an unmigrated legacy design doesn't propagate the old shape), then deep-copies `shapeEntries` and every colorway with fresh colorway ids (remapping `activeColorwayId` to the corresponding new id).
  - `src/state/appState.js` — added `colorways: []` (in-memory mirror of the open design's colorway list, same role `appState.designs` plays for the library) and `activeColorwayId: null`. Deliberately no live `shapeEntries` field on `appState` — the shape is always `Array.from(appState.cells.keys())` at the moment it's needed, so there's nowhere for a separate copy to drift out of sync with what's actually drawn.
  - `main.js` — `openDesign()` materializes `appState.cells` from `design.shapeEntries` + the active colorway's `colorEntries` (via `materializeColorwayCells`) instead of the old flat `entriesToCells(design.cellEntries)`; `persistCurrentDesign()` decomposes the live `cells` Map, prunes every colorway to the resulting shape, folds the decomposed colors back into the active colorway, and saves `shapeEntries`/`colorways`/`activeColorwayId` instead of a flat `cellEntries` field.
  - `src/ui/editorView.js` — added a `#colorway-controls` group (select + New/Rename/Delete) and its handlers. `switchColorway(newColorwayId)`: decomposes whatever's currently drawn, prunes+folds it back into the colorway being left, materializes the target colorway against the (now up-to-date) shared shape, clears history (a patch's before/after colors are only meaningful against the colorway they were recorded under — same reasoning Phase 4 already applies to design switches and Phase 5.x applies to resize), and triggers an immediate save. `handleColorwayNew` seeds the new colorway as a copy of the active one's current colors (there's no separate "duplicate" action — create *is* duplicate, scoped to one pattern) then reuses `switchColorway`. `handleColorwayDelete` is blocked at exactly one remaining colorway (button disabled) and switches to the first remaining one if the active colorway was deleted. `resolveColor` now returns `UNASSIGNED_SWATCH.hex` for `colorId === null` — no `canvasRenderer.js` change needed, since it already branches on cell *presence*, not colorId truthiness, so a `{colorId: null}` entry already took the filled-bead path, not the empty-dot path. `regenerateGrid`/`handleClear` now also blank every colorway's `colorEntries` (a shared-shape mutation, not just what's on screen) and both use a new `confirmClearMessage()` that names the colorway count explicitly when there's more than one to lose (previously-existing single-colorway wording is unchanged in the common case). `applyResize` now applies the identical anchor-offset remap to every colorway's `colorEntries`, not just the active `cells` Map — otherwise switching to an untouched colorway after a resize would show colors sitting at pre-resize coordinates. The resize dialog's live "N beads will be removed" warning (`resizeDialog.js`, `countCellsLost`) needed **no change** — it already runs against `appState.cells`, and under shared shape the active colorway's key set *is* the shape, so the count is already correct for every colorway at once; confirmed via testing rather than assumed, since it's the one place a bug would have been silent (an under-warn).
  - `src/export/wordChart.js` — added an exported `UNASSIGNED` `Symbol`, distinguishing "cell absent" (a genuinely blank run, `colorId: null`, unchanged meaning since Phase 5) from "cell present with `colorId: null`" (occupied in the shared shape, unassigned in this colorway) as a run type of its own. Added `unassignedCount` to the return value, folded into `totalBeadCount` so a colorway missing colors doesn't silently undercount its own bead total. `colorCounts`/existing Phase 5 behavior for real colors and genuine blanks is untouched.
  - `src/ui/printView.js` — `formatRun` gained a branch rendering an `UNASSIGNED` run as `"{count} ??"`; `buildMaterials` prepends a `.print-warning` line (`"⚠ N bead(s) in this colorway have no color assigned yet."`) above the materials table whenever `chart.unassignedCount > 0`, so it's the first thing visible on a printout of a colorway nobody's finished coloring in — printing itself is never blocked, matching Phase 5's existing philosophy for an empty design (a clear notice, not a hard stop).
  - `index.html`/`style.css` — `#colorway-controls` (select + New/Rename/Delete) added to `#controls`, styled consistent with `#tool-toggle`/`#history-controls`'s existing compact grouped-button convention (plan's default choice over a tab strip — flagged for a look on the real iPad before treating as final); `.print-warning` styled as an amber notice box.
  - `node:test` coverage: 94 tests passing (14 new — `colorwaySync.test.js`'s materialize/decompose/prune/round-trip cases; `resizeGrid.test.js`'s new `resizeKeyList`/`resizeColorEntries` cases mirroring the existing anchor/grow/shrink combinations; `migrateDesign.test.js`'s legacy-wrap/already-migrated/empty-design cases; `wordChart.test.js`'s mixed real-color/blank/unassigned-run case and an `unassignedCount`/`totalBeadCount` hand-counted fixture; plus all of Phases 1–5's existing suites unchanged, including `resizeCells`/`countCellsLost`'s pre-existing cases confirming the `remapEntries()` refactor is behavior-preserving).
  - Verified in headless Chromium (Playwright, same local-`http.server` + exact-cell-click approach as prior phases — cell coordinates computed by reimplementing `peyoteCellOriginMm`/`fitViewportToGrid`/`worldToScreen`'s math directly in the test script rather than importing the source, since this was a standalone driver script outside the app's own module graph): drawing in colorway A, creating colorway B (confirmed to start pixel-identical to A at a shared cell), drawing a genuinely new cell in B only, recoloring an existing shared cell in B, and erasing a different shared cell in B, then switching back to A confirmed all three shared-shape behaviors by pixel sampling: the new cell B added appears in A as the unassigned placeholder color (shape is shared), the recolor in B did not change A's color at that cell (colors are per-colorway), and the erase in B removed the cell in A too (shape shrink propagates). Rename updated the select label; delete-while-not-last removed the colorway and fell back to the remaining one; delete disabled correctly at exactly one colorway remaining. Separately verified: a design reloaded after these operations still had the expected bead present (persistence round-trips `shapeEntries`/`colorways` correctly); a bead-type change (regenerate) left the colorway list's count/names intact while wiping contents. A second script seeded a legacy Phase-4/5-era record directly into IndexedDB (flat `cellEntries`, no `colorways` field) before booting — confirmed it appears in the library, opens with exactly one migrated colorway, still renders its original pattern intact, and that the re-saved record now carries `colorways`/`shapeEntries` with `cellEntries` gone (migration is not re-run on subsequent boots). A third check confirmed the print view's unassigned-cell warning and `??` chart markers appear only when the printed colorway actually has unassigned cells, and disappear for a fully-colored single-colorway design (no regression from Phase 5's existing verified print behavior). A regression script confirmed draw/undo/redo and the resize dialog's shrink/grow confirm-guard flow are unaffected by the shared-shape plumbing in the common single-colorway case.
  - **Not yet done**: real iPad pass (plan step 11 — `#colorway-controls` at touch size in an already fairly full `#controls` toolbar; whether switching colorways feels instant with no visible flash; whether the unassigned-cell placeholder fill is visually distinct enough while actually stitching-testing a design, not just in a pixel diff; confirming a real pre-Phase-6 design already saved on the iPad opens correctly post-migration before relying on this in practice). The two "open, low-stakes" implementation calls the plan flagged (flat unassigned-cell fill vs. a hatch pattern; select+icons vs. a tab strip for the colorway switcher) were both taken as-planned and are easy to revisit later without touching the data model.
  - Next step: **Phase 7 (remaining tools)** per CLAUDE.md's Phase Plan — fill, color-replace, cut/copy, mirror, photo trace overlay, grid/canvas orientation toggle. No Phase 7 plan written yet. The "Bead-type conversion" backlog item (Later/optional section above) remains unscheduled, still blocked on real Miyuki catalog color data.

- Status as of two sessions ago: **Two user-reported fixes, outside the phase plan, done between Phase 5 and Phase 6.**
  - **Grid orientation transpose.** The user flagged that the peyote grid was rendering "sideways" — jagged edges on the left/right instead of top/bottom. After a few rounds of clarification (including the user sharing a screenshot of their actual designs from the prior app and a reference diagram of flat even-count peyote), the conclusion was: this isn't a bounding-box/aspect-ratio issue, it's that rows and columns were fully swapped relative to the standard diagram. Fixed by transposing `src/grid/peyote.js`'s `peyoteCellOriginMm`/`generatePeyoteGrid`/`peyoteCellAtPoint` — literally swapping x and y (row now drives `xMm` using `beadHeightMm`, offset by `beadWidthMm/2` on odd rows; col now drives `yMm` using `beadWidthMm`). Rows run horizontally now (each row is still one physically-flat thread pass, unchanged for `wordChart.js`'s purposes — it groups/reads by row exactly as before, print output just now reflects rows read as vertical strips left-to-right/right-to-left rather than horizontal bands top-to-bottom); columns run vertically. **Deliberate, confirmed consequence:** the internal `rows` axis (id="rows", `appState.rows`) controls the pattern's on-screen width and the internal `cols` axis (id="cols", `appState.cols`) controls its height — this was explicitly surfaced to and confirmed by the user, not an accidental side effect. **Superseded (2026-08-10):** the on-screen *labels* for these two fields were swapped display-layer-only (top bar, resize dialog legends, print/export header line and "Row N"→"Col N" chart lines — see `src/ui/resizeDialog.js`'s header comment) so the visible word "Rows" now sits next to whichever field controls height and "Cols" next to whichever controls width, matching the more intuitive rows=vertical/cols=horizontal convention. Nothing about `appState.rows`/`appState.cols`, `peyoteCellOriginMm`, or any grid/tool/render logic changed — only user-facing copy. `src/render/canvasRenderer.js`'s visible-range culling (`rowRange`/`colRange`) and per-cell screen-extent calc were updated to match (they had hardcoded assumptions about which of `beadWidthMm`/`beadHeightMm` applies to which screen axis). `src/interaction/pointerRouter.js` and `src/render/viewport.js` needed no changes (orientation-agnostic). `src/test/grid/peyote.test.js` updated for the transposed formula. Verified visually via headless-Chromium screenshot (Playwright): a 4-row × 10-col empty grid renders as 4 columns marching across the canvas, each vertically offset from its neighbor, giving the expected jagged top/bottom silhouette with straight left/right edges.
  - **Resize preserves the existing design.** Previously, changing Rows/Cols and clicking Generate always cleared the pattern (confirm-guarded). The user wanted the existing design kept, with control over which side absorbs added/removed rows/cols, and a required confirmation only when something would actually be lost. Built as: `src/state/resizeGrid.js` — pure `resizeCells(cells, oldRows, oldCols, newRows, newCols, rowAnchor, colAnchor)` and `countCellsLost(...)`, where anchor is `'start'` (existing content pinned to index 0, change happens at the far end) / `'end'` (content pinned to the last index, change happens at index 0) / `'both'` (split, odd unit goes to the `'end'` side) — axis-agnostic on purpose, so it doesn't need to know rows are horizontal and cols are vertical. `src/ui/resizeDialog.js` — `promptResizeOptions({cells, oldRows, oldCols, newRows, newCols})`, a `Promise`-returning wrapper around a native `<dialog>` (`#resize-dialog` in index.html): shows a row-side picker (Left/Right/Both) only if rows changed and a col-side picker (Top/Bottom/Both) only if cols changed — those UI labels map to the axis-agnostic `start`/`end`/`both` anchors via a small lookup table specific to the dialog module, keeping resizeGrid.js itself orientation-agnostic. A warning line ("This will permanently remove N placed beads") recomputes live via `countCellsLost` on every radio change, and the confirm button relabels to "Remove & Resize" (red/destructive style) whenever the currently-selected combination would lose anything — this live warning **is** the required confirmation, there's no separate `window.confirm()` on top. `src/ui/editorView.js`: the Generate button (relabeled "Resize") now calls `handleResizeClick` instead of `regenerateGrid` — parses the new rows/cols, no-ops if unchanged, resizes immediately with no dialog if the design has no placed cells yet (nothing to lose or choose sides for), otherwise awaits `promptResizeOptions` and applies the result via `applyResize()` (remaps cells, rebuilds `gridParams`, clears history since old undo/redo patches reference now-invalid coordinates, refits the viewport, persists as a new preference default, triggers immediate save); cancelling reverts the Rows/Cols number inputs back to the design's actual current values. Bead type changes are unaffected — they still go through the original `regenerateGrid()` (full clear, `window.confirm()`-guarded), since preserving cell data across a bead type change is the separate, still-unscheduled "Bead-type conversion" backlog item (see Later/optional section) and wasn't in scope here. New `src/test/state/resizeGrid.test.js` (9 tests: growing/shrinking with each anchor, cross-axis independence, no-op on unchanged dimensions, `countCellsLost` matching `resizeCells`' actual drop count). Full suite: 80 passing. Verified in headless Chromium (Playwright, exact-cell clicks computed via the app's own `peyote.js`/`viewport.js` math, matching prior phases' approach): resizing an empty grid skips the dialog entirely; growing rows with cells present shows only the row-side fieldset with no warning, and choosing "Left" correctly shifts an existing bead's coordinates (confirmed via pixel sampling before/after); shrinking shows a live warning that recalculates correctly as the anchor selection changes; cancelling leaves both the design and the Rows/Cols inputs exactly as they were.
  - **Not yet done**: neither fix has been checked on a physical iPad yet (native `<dialog>`/`showModal()` support, touch interaction with the new radio-button dialog, and the new width/height mapping's feel in actual use should all be confirmed there before relying on it).
  - Next step: **Phase 6 (colorways)**. Plan written this session as `.work/phase-6-implementation-plan.md` — no Phase 6 code written yet. Core architecture decision (confirmed by the user this session): colorways of one pattern share a single physical bead layout ("shape") — draw/erase always edit that shared shape, visible in every colorway immediately; only the color assigned to an occupied cell is stored per-colorway. Chosen over the simpler alternative (fully independent per-colorway cell maps, seeded by copying on creation) because that alternative can silently let colorways drift into different shapes over time, which undercuts the point of grouping them as "the same pattern" — matches Decision #8 ("same grid, different color-to-cell mapping only") literally. Consequence worth remembering going in: occupancy and color become two separate concepts instead of one, so the plan required tracing the shared-shape model through resize, regenerate, undo/redo's history-clearing rule, word chart export, and a migration path for the flat-`cellEntries`-shaped designs already saved from Phases 4–5 (including real ones already on the iPad) — see the plan's "Build order + verification" section for the full sequencing. Two implementation details were deliberately left open in the plan (not blocking): the exact visual treatment for an occupied-but-not-yet-colored cell, and whether the colorway switcher UI is a select+icon-buttons cluster (plan's default, matching `#tool-toggle`'s existing style) or a tab strip — both are cheap to change later since they don't touch the data model.

- Status as of two sessions ago: **Phase 5 complete.** Built per `.work/phase-5-implementation-plan.md`'s file breakdown and build order:
  - `src/export/wordChart.js` — `buildWordChart(cells, rows, cols)`, pure. Row-length-encodes each row into `{colorId, count}` runs (a run of empty cells uses `colorId: null`), and aggregates per-color totals (`colorCounts`, first-appearance order) plus `totalBeadCount` across the whole design. `displayRuns(chartRow)` returns a row's runs reversed for odd `rowIndex`, unchanged for even — mirrors peyote's back-and-forth thread path (matches `peyote.js`'s existing `row % 2 === 1` offset rule); `rows` itself always stores runs in canonical left-to-right order.
  - `src/export/colorCodes.js` — `assignColorCodes(colorCounts)`, pure. Spreadsheet-style `A, B, ..., Z, AA, AB, ...` codes, sorted most-used-first (ties keep first-appearance order via stable sort), only for colors actually present in the pattern. Returns a plain `Map<colorId, code>`.
  - `src/ui/printView.js` — `mountPrintView(appState)` builds the `#print-view` overlay's header (pattern name, bead type/size, rows×cols, finished size in both mm and in via `formatLength`), materials/legend table (code, swatch, name, count per `colorCounts` entry, plus a total-beads row), and row-by-row word chart (`Row {n} {→|←}: {runs as "{count}{code}" or "{count} blank"}`) from the current design's live `appState` — reads once at mount, writes nothing back, no hooks into main.js. `totalBeadCount === 0` swaps the materials/chart sections for a single "No beads placed yet" message. Wires its own Close/Print (`window.print()`) buttons and owns showing/hiding `#print-view` itself.
  - `src/ui/editorView.js` — added a "Print / Export" button that calls `mountPrintView(appState)` directly; no new hook needed since the overlay is self-contained and ephemeral (matches the plan's "no hooks into main.js needed").
  - `index.html`/`style.css` — `#print-view` (fixed, full-screen overlay, same `hidden`-attribute toggle pattern as `#library-view`/`#editor-view`) containing `#print-toolbar` (Close/Print) and `#print-content` (header/materials/chart); `@media print` rules hide `#library-view`/`#editor-view`/`#print-toolbar` and let only `#print-view` content through; `.word-chart-row` uses `break-inside: avoid` so a run of text never splits across a page break, with every-10th-row bold emphasis for keeping place on a long printout.
  - `node:test` coverage: 71 tests passing (11 new — `wordChart.test.js` covers single-color-run collapsing, alternating single-cell runs, leading/trailing/interior blank runs, hand-counted `colorCounts`/`totalBeadCount`, an entirely empty design, and `displayRuns`' even/odd reversal; `colorCodes.test.js` covers the `A` most-used case, a 30-color fixture rolling into `AA`/`AB`, an absent/zero-use color never getting a code, and the `Map` return type; plus all of Phases 1–4's existing suites unchanged).
  - Verified in headless Chromium (Playwright, same local-`http.server` approach as prior phases, driven with exact-cell clicks computed by importing the app's own `peyote.js`/`viewport.js` math rather than approximating coordinates): a hand-designed 3-row × 9-col pattern (3 red / 2 blank / 1 blue / 3 blank on row 0; 3 blank / 2 red / 4 blank on row 1, drawn left-to-right but displayed reversed since it's an odd row; row 2 all blank) produced word-chart lines matching the hand-computed expected text exactly, including the →/← direction indicators and the odd-row run reversal; the materials table's codes (red=A as most-used, blue=B), names, per-color counts, and total (6) all matched; print-media emulation (`page.emulateMedia({media:'print'})`) confirmed `#library-view`/`#editor-view`/`#print-toolbar` compute to `display: none` and `#print-view` remains visible; closing the overlay returned to a still-interactive editor (drawing another cell afterward still worked); a zero-bead design showed the empty-state message with no materials rows or chart lines; no console/page errors across the run. Separately timed `buildWordChart` directly in Node against a 300×200 grid with 5,000 placed cells (reusing Phase 2/3's perf-fixture scale) at ~14ms, confirming no pathological blowup — cost is one pass over `rows × cols`, independent of how sparse the pattern is.
  - **Verified on physical iPad**: user confirmed Phase 5 works as expected on-device (Print/Export button, print/share sheet flow, printout legibility).
  - Next step: **Phase 6 (colorways)** per CLAUDE.md's Phase Plan — same-grid/alternate-palette variant (Decision #8). No Phase 6 plan written yet. Note the "Bead-type conversion" backlog item (Later/optional section above) is conceptually adjacent but distinct (different bead type on the same grid, vs. Phase 6's same bead type/different palette) and still blocked on real Miyuki catalog color data before its remapping question can be resolved — doesn't block starting Phase 6.

- Status as of two sessions ago: **Phase 4 complete.** Built per `.work/phase-4-implementation-plan.md`'s file breakdown and build order:
  - `src/storage/db.js` — generic IndexedDB promise wrapper (`openDatabase`, `get`/`getAll`/`put`/`del`); creates `designs` (keyPath `id`) and `preferences` (keyPath `id`) object stores on first open.
  - `src/storage/designStore.js` — `listDesignsSorted` (sorted by `order`), `createDesign` (computes `order` as current max + 1, or 0 for an empty library), `saveDesign` (bumps `updatedAt`), `deleteDesign`, `duplicateDesign` (new id, independent deep-copied `cellEntries`, name suffixed " copy", placed after the current max order). IDs come from `src/storage/id.js`'s `generateId()`, not `crypto.randomUUID()` directly — see the bug note below.
  - `src/storage/preferencesStore.js` — `getPreferences` (built-in defaults — `mm`, `delica11`, 20×20 — when no row exists yet), `savePreferences`, single row keyed `'global'`.
  - `src/storage/debounce.js` — `debounce(fn, delayMs)` with `.flush()`, exactly per the plan's reference implementation.
  - `src/state/cellStore.js` — added `cellsToEntries`/`entriesToCells` (Map ⇄ plain array), the one place that shape conversion happens, per Decision #5's future JSON export.
  - `src/state/designOrder.js` — `orderForInsertAt(sortedList, targetIndex)`, pure fractional-order calculation for drag-reorder.
  - `src/state/appState.js` — `createAppState()` factory, lifted out of main.js's old inline object literal; now spans `view` ('library' | 'editor'), `db`, `preferences`, `designs` (in-memory list), `currentDesignId`, plus the existing per-design/editor fields (`beadTypeKey`, `rows`, `cols`, `units`, `gridParams`, `viewport`, `tool`, `selectedColorId`, `cells`, `history`).
  - `src/interaction/pointerRouter.js` — `cells` param → `getCells()` getter, since `appState.cells` is now reassigned to a new Map on every design open; the router previously closed over the first design's Map by value and would have silently kept writing into a discarded Map for every design after the first.
  - `src/ui/editorView.js` — today's canvas/tool/palette/grid wiring lifted out of main.js into `mountEditorView(appState, hooks)` / `unmount()`. Distinguishes `deriveGridAndRender()` (no clearing — used once when a design opens, so its existing cells survive) from `regenerateGrid()` (confirm-guarded clear — used when the user changes bead type/rows/cols or hits Generate). Talks to main.js only through hooks: `onCellsChanged` (debounced autosave trigger, fired by draw/erase/undo/redo), `onImmediateSave` (regenerate/Clear), `onPreferencesChanged(patch)` (regenerate and the units toggle both write back new defaults), `onBack`.
  - `src/ui/libraryView.js` — `mountLibraryView(callbacks)` renders rows (name, relative "updated" time, drag handle, rename/duplicate/delete icon buttons) into `#library-list`, wires "+ New". Delete is `confirm()`-guarded; rename uses `prompt()`. Drag-reorder: pointer capture is taken on `#library-list` itself, not on the dragged row/handle — capturing the row directly broke the instant the drag's own `insertBefore` reparented it (Chromium silently releases capture when the captured element is moved mid-gesture, discovered via Playwright testing this session, see below). The list container never moves, so capturing there survives the whole drag; `pointerdown` uses `e.target.closest('.library-drag-handle')` to identify the dragged row via delegation.
  - `main.js` — shrunk to an app shell: `boot()` opens the DB, loads preferences + the sorted design list, mounts the library view. `openDesign(design)` loads a record's fields into `appState`, creates a fresh `history`, mounts the editor view with autosave hooks. `persistCurrentDesign()` always reads live `appState` (idempotent regardless of when/how often it's called — a redundant call just re-writes the same current values). A debounced save (800ms) is created per design-open and discarded on `backToLibrary()`; `visibilitychange`(hidden)/`pagehide` call `.flush()` for the iPad-backgrounding case. New/Rename/Duplicate/Delete/Reorder handlers keep `appState.designs` in sync in-memory (no full re-fetch from IndexedDB after each mutation).
  - `index.html`/`style.css` — existing controls wrapped in `#editor-view`, new `#library-view` (header + "+ New" + `#library-list` + empty-state message) added; both toggle via the `hidden` attribute. Library row layout, drag-handle affordance, and a `.dragging` visual state (reduced opacity + tint) added to style.css.
  - `node:test` coverage: 60 tests passing (`cellStore`'s new entries round-trip cases, `designOrder`, `debounce` with mock timers — `apis: ['setTimeout']` only, `'clearTimeout'` is not a valid mock-timers API name on this Node version — plus all of Phases 1–3's existing suites unchanged). `db.js`/`designStore.js`/`preferencesStore.js` have no `node:test` coverage since Node has no native `indexedDB` — verified in headless Chromium instead, per the plan.
  - Verified in headless Chromium (Playwright, same local-`http.server` approach as prior phases): boots into an empty library view; "+ New" opens a fresh design directly into the editor (no name-entry modal); a drawn stroke autosaves (confirmed via reload + non-blank-canvas pixel check, not just button/list state) and is restored on reopen; rename/duplicate/delete all behave correctly, including the delete confirm guard; drag-reorder (three-plus designs) both re-renders correctly and survives a reload; regenerate/units-toggle both write new global preference defaults and a subsequently-created design inherits them; dispatching `visibilitychange` (hidden) immediately after a stroke — without waiting for the 800ms debounce — still persists it, confirming the iPad-backgrounding flush path works; no console/page errors across either pass.
  - **Bug found after this session's Playwright pass (not caught by it) and fixed**: `crypto.randomUUID()` only exists in a secure context — HTTPS, or the special-cased `localhost` — and the user hit `crypto.randomUUID is not a function` clicking "+ New" when testing over the Mac's LAN IP in Safari (plain HTTP, non-`localhost`, so `window.isSecureContext` is `false` and `crypto.randomUUID` is `undefined` entirely, not just throwing). `crypto.getRandomValues` has no such restriction, so `src/storage/id.js`'s `generateId()` builds a UUID v4 manually on top of it, falling back to `crypto.randomUUID()` when it *is* available. Confirmed the fix with Playwright loading the app over the LAN IP directly (`window.isSecureContext` false, `crypto.randomUUID` undefined, "+ New" now works) — worth remembering for any future feature that reaches for a browser API that gates on secure-context, since this project's iPad testing is routinely done over plain-HTTP LAN, not `localhost` or HTTPS.
  - **Not yet done**: real iPad pass (plan step 14 — touch drag-reorder feel, Pencil/finger comfort on all the new buttons, backgrounding mid-stroke on-device, `confirm()`/`prompt()` dialog behavior under iPad Safari); a few of the plan's step-15 edge cases (rapid Back→New→Back→New tapping racing the debounce/flush logic; drag-reorder at the scrolled edges of a long library list). Both open questions from the plan were already confirmed by the user before this session (see the old Phase 3 status entry below) and implemented accordingly: the app always boots into the library view, and drag-reorder is real pointer-based dragging, not up/down buttons.
  - Next step: **Phase 5 (print/export instructions)** — row-by-row word chart generation, printable layout. Plan written this session as `.work/phase-5-implementation-plan.md`; no Phase 5 code written yet. Key points: a pure `src/export/wordChart.js` (row-length-encodes each row into color/blank runs, aggregates per-color totals; also exports `displayRuns()`) and `src/export/colorCodes.js` (spreadsheet-style A/B/.../AA pattern-local color codes, most-used first) feed a new `src/ui/printView.js` overlay mounted over the editor — header, materials/legend table, row-by-row chart — printed via native `window.print()` + `@media print` CSS, not a PDF library (Decision #2). Nothing new is persisted; this is distinct from Decision #5's future JSON export/import (data portability) — Phase 5 is an output-only stitching reference. Both open questions from the plan were confirmed by the user this session: printed rows alternate reading direction per row (Row 1 left-to-right, Row 2 right-to-left, ...) rather than always left-to-right, matching peyote's actual back-and-forth thread path — this lines up with `peyote.js`'s existing `row % 2 === 1` offset rule, and each printed line gets a →/← indicator so the alternation is explicit; and no rendered picture-chart graphic on the printout in v1 (word chart only, matching CLAUDE.md's Phase 5 line literally — a static `canvasRenderer.js` snapshot is a contained follow-up if needed later).

- Status as of two sessions ago: **Phase 3 complete.** Built per `.work/phase-3-implementation-plan.md`'s file breakdown and build order:
  - `src/state/strokePatch.js` — pure accumulator for one in-progress stroke (`createStrokePatch`/`recordCellChange`/`strokePatchToArray`); a cell touched more than once in a stroke collapses to one entry (first `before`, latest `after`), per the plan's dedupe decision.
  - `src/state/historyStore.js` — `createHistory`/`pushPatch`/`undo`/`redo`/`canUndo`/`canRedo`/`clearHistory`, pure w.r.t. the `history`/`cells` passed in. `pushPatch` no-ops on an empty patch and clears the redo stack; history is capped at `MAX_HISTORY_DEPTH = 100` (oldest patch dropped past the cap).
  - `src/tools/drawTool.js` / `src/tools/eraseTool.js` — `applyDrawAtCell`/`applyEraseAtCell` widened from a boolean return to `{row, col, before, after} | null` (before is whatever `cells.get()` held, possibly `undefined`); existing truthy/falsy call sites needed no changes.
  - `src/interaction/pointerRouter.js` — `drawStroke` gained a `patch` field; `applyToolAtWorld` now records each changed cell into it via `recordCellChange`. Both places a stroke can end (`handlePointerEnd`, and the second-pointer-lands-mid-stroke abort branch in `handlePointerDown`) now route through a new `commitStroke()` helper that fires the new `onStrokeCommitted` callback with `strokePatchToArray(patch)`, only if non-empty.
  - `main.js`/`index.html`/`style.css` — `appState.history = createHistory()`; `#undo-button`/`#redo-button` next to Clear, wired to `historyStore.undo`/`redo` + `scheduleRedraw` + `updateHistoryButtons`; Cmd/Ctrl+Z and Cmd/Ctrl+Shift+Z keyboard shortcuts (ignored while an `<input>` is focused); `regenerateGrid()` and the Clear handler both call `clearHistory(appState.history)`; `attachPointerRouter(...)` gained `onStrokeCommitted: (patch) => { if (pushPatch(...)) updateHistoryButtons(); }`. Buttons use native `disabled`, not the `aria-pressed` toggle pattern (undo/redo are one-shot actions, not a selected mode, per the plan).
  - `node:test` coverage: 50 tests passing (`strokePatch`, `historyStore`, updated `drawTool`/`eraseTool` for the new return shape, plus all of Phases 1–2's existing suites unchanged).
  - Verified in headless Chromium (Playwright, same local-`http.server` approach as Phases 1–2): buttons start disabled; a single tap enables Undo and leaves Redo disabled; Undo reverts the stroke (confirmed via pixel sampling, not just button state) and enables Redo; Redo reapplies it and the pixel matches the original; a new stroke after an Undo clears the Redo stack; a scribbled back-and-forth drag over the same cells commits as one patch and a single Undo reverts the entire stroke to its pre-stroke canvas state (full-canvas pixel snapshot compared, not just spot-checked); regenerating and Clear both disable both buttons even when history existed before; Cmd/Ctrl+Z undoes and Cmd/Ctrl+Shift+Z redoes, and — specifically — doing so while focus is inside the Rows number input is a no-op (native text-field undo isn't hijacked); a dense stroke on a 200×300 grid commits and undoes in ~65ms, confirming patch cost scales with cells touched in the stroke, not grid size.
  - **Verified on physical iPad**: user confirmed Phase 3 works as expected on-device.
  - Next step: **Phase 4 (save/load + project library)**. Plan already written in `.work/phase-4-implementation-plan.md`. Per that plan's "forward-compat note," Phase 4 must additionally call `clearHistory(appState.history)` on design open/switch (a patch's row/col coordinates are only meaningful against the design they were recorded under) — not yet done, since no Phase 4 code exists yet. That plan also already accounts for the `pointerRouter.js` `cells` → `getCells()` getter change it introduces.

- Status as of two sessions ago: **Phase 2 complete.** Built per `.work/phase-2-implementation-plan.md`'s file breakdown and build order:
  - `src/palette/colorLibrary.js` — placeholder 16-swatch palette (NOT verified Miyuki color numbers — see "Open question" in the Phase 2 plan; proceeded with the plan's own recommendation to build the mechanism now and swap in real catalog data later, since that's a data-only change).
  - `src/state/cellStore.js` — pure `Map<"row,col", { colorId }>` helpers (`cellKey`/`setCell`/`clearCell`/`getCell`).
  - `src/grid/peyote.js` — added `peyoteCellAtPoint` (inverse of `peyoteCellOriginMm`), round-trip tested against it for every cell in a sample grid, both even and odd rows.
  - `src/tools/drawTool.js` / `src/tools/eraseTool.js` — pure `applyDrawAtCell`/`applyEraseAtCell`, each returns whether a cell actually changed so redraws are skipped on no-op drag re-entry.
  - `src/render/canvasRenderer.js` — `drawPeyoteGrid` now takes optional `cells`/`resolveColor` params and fills occupied cells before stroking outlines; stays ignorant of what a "color library" is.
  - `src/interaction/dragTrace.js` — `interpolatedWorldPoints`, fills in intermediate points between pointermove samples so fast drags don't skip cells.
  - `src/interaction/panZoom.js` renamed to `pointerRouter.js` per the plan's centralized-routing decision: two touch/pen pointers → pan/zoom (unchanged math), exactly one touch/pen pointer or mouse-left-drag → draw/erase, Space+mouse-left-drag → pan (dev fallback), plain wheel → pan, ctrl+wheel → zoom-to-cursor. A second touch pointer landing mid-stroke cancels the draw stroke and hands off to pan/zoom.
  - `main.js`/`index.html`/`style.css` — `appState` extended with `tool`, `selectedColorId`, `cells`; draw/erase tool buttons, a color swatch strip (rebuilt on bead-type change), and a Clear button wired in. Regenerating the grid or switching bead type clears `cells` with a `confirm()` guard when non-empty (prior-app pain point #4).
  - `node:test` coverage: 37 tests passing (`cellStore`, `peyote` incl. `peyoteCellAtPoint` round-trip, `drawTool`, `eraseTool`, `dragTrace`, plus Phase 1's existing suites).
  - Verified in headless Chromium (Playwright, driven via a synthetic script against a local `python3 -m http.server`, same approach as Phase 1 since no project run-skill exists yet): mouse tap places exactly one bead, erase tool clears it, selecting a palette swatch changes the drawn color, mouse-drag paints a continuous line with no gaps, Clear button empties the pattern (confirm auto-accepted), plain wheel pans the view, ctrl+wheel zooms, synthetic single-finger touch (dispatched `PointerEvent`s with `pointerType: 'touch'`) draws a cell, and a second synthetic touch landing mid-stroke hands off to pan/zoom without leaving a stray bead between the two fingers. Also verified: a dense scribbled drag on a 300×200 grid that deliberately exits/re-enters the canvas and grid bounds completes (~1.1s) with no thrown errors, and rapid tool-switching mid-drag doesn't throw.
  - **Verified on physical iPad** (Safari, over LAN via `python3 -m http.server` + Mac's LAN IP, not yet installed to home screen): draw, erase, pinch-to-zoom, and pan all confirmed working well by the user with real touch/Pencil input.
  - **New finding from iPad testing — orientation:** the app currently renders/fits the grid to whatever the canvas's aspect ratio is (landscape on the iPad in its current orientation), but the user stitches in a vertical/portrait layout most of the time and will likely want that as the default working orientation. Not yet designed or scoped — logged as a backlog item under Phase 6 (Remaining tools) as a grid/canvas orientation toggle. Open question for whenever that's picked up: does "orientation" mean rotating the grid's row/col axes (so the offset-row stagger runs left-right instead of top-bottom), locking the app's layout to portrait regardless of device rotation, or both — needs clarifying before design.
  - Still not tested: home-screen standalone mode (no Safari chrome) — worth checking once orientation is settled, since standalone sizing/safe-area behavior is a separate concern from the LAN-Safari testing done this session.
  - No project-level run skill exists yet for this app; still recommend `/run-skill-generator` to capture the dev-server + Playwright verification loop (used again this phase) so future sessions don't re-derive it from scratch.
  - Phase plan updated this session: undo/redo added as its own **Phase 3** (ahead of save/load), pushing the old Phase 3 (save/load + library) to **Phase 4**, print/export to Phase 5, colorways to Phase 6, remaining tools to Phase 7. See Decision #12 and the Phase Plan section above.
  - Next step: **Phase 3 (undo/redo)**. Plan is in `.work/phase-3-implementation-plan.md` — no code written yet. Key points: undo unit is a whole stroke (not a single cell), history stores diffs/patches (not full-grid snapshots), a cell touched more than once in one stroke gets a single deduped patch entry, `applyDrawAtCell`/`applyEraseAtCell` widen their return value from boolean to `{row,col,before,after} | null` (backward-compatible at existing truthy/falsy call sites), history is in-memory only and cleared on regenerate/Clear (never persisted). Plan flags that Phase 4, when built, will additionally need to clear history on design open/switch.
  - The save/load + project library plan (now Phase 4) was already written this session as `.work/phase-3-implementation-plan.md` before the renumbering above — it has since been renamed to `.work/phase-4-implementation-plan.md` and its internal phase references updated to match. No Phase 4 code written yet. Two open questions in that plan were confirmed by the user: the app always boots into the library view (never auto-resumes the last-open design), and drag-to-reorder in the library is a real pointer-based drag (not up/down buttons). It also flags a required fix while porting Phase 2 code: `pointerRouter.js`'s `cells` param must become a `getCells()` getter, since switching designs will reassign `appState.cells` to a new Map and the router currently closes over the old one by value.